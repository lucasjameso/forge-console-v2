---
phase: quick
plan: 260323-16z
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useContentReviews.ts
  - src/hooks/useActivityLog.ts
  - src/pages/ContentPipeline.tsx
  - src/pages/ActivityLog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Add Content modal inserts a new row into content_reviews table and shows success toast"
    - "Log Activity form inserts a new row into activity_log table and shows success toast"
    - "Both forms reset and close after successful insert"
    - "Query caches invalidate so fresh data appears without manual refresh"
  artifacts:
    - path: "src/hooks/useContentReviews.ts"
      provides: "useCreateContent mutation hook"
      exports: ["useCreateContent"]
    - path: "src/hooks/useActivityLog.ts"
      provides: "useLogActivity mutation hook"
      exports: ["useLogActivity"]
  key_links:
    - from: "src/pages/ContentPipeline.tsx"
      to: "src/hooks/useContentReviews.ts"
      via: "useCreateContent mutation called in handleCreate"
      pattern: "useCreateContent"
    - from: "src/pages/ActivityLog.tsx"
      to: "src/hooks/useActivityLog.ts"
      via: "useLogActivity mutation called in handleLogSubmit"
      pattern: "useLogActivity"
---

<objective>
Wire the Add Content modal and Log Activity form to Supabase via React Query mutations.

Purpose: Both forms currently only show a toast or reset state without persisting data. This connects them to the database.
Output: Two new mutation hooks and updated page handlers that insert data and invalidate caches.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useContentReviews.ts
@src/hooks/useActivityLog.ts
@src/pages/ContentPipeline.tsx
@src/pages/ActivityLog.tsx
@src/types/database.ts

<interfaces>
<!-- Existing mutation pattern from useContentReviews.ts (follow this exactly): -->
```typescript
export function useUpdateContentStatus() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: async ({ id, status, feedback }: { ... }) => {
      if (!isSupabaseConfigured) return
      const { error } = await (supabase as any).from('content_reviews').update({...}).eq('id', id)
      if (error) throw error
    },
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: ['content-reviews'] })
      toast.success(...)
    },
    onError: (error: Error) => {
      toast.error(...)
    },
  })
}
```

<!-- ContentReview Insert type (from Database interface): -->
<!-- Omit<ContentReview, 'id' | 'created_at'> -- all fields except id and created_at -->
<!-- Required fields: post_title, week_number, day_label, status, content_type, revision, slide_count, export_paths, excalidraw_paths, platforms -->

<!-- ActivityEntry Insert type: -->
<!-- Omit<ActivityEntry, 'id' | 'created_at'> -->
<!-- Fields: session_type (SessionType), project (string|null), tool (string|null), summary (string), metadata (Record<string,unknown>|null) -->

<!-- NewContentState shape (from ContentPipeline.tsx): -->
<!-- { title, body, content_type, scheduledDate, dayLabel, platforms, series, notes } -->

<!-- ActivityLog form state (from ActivityLog.tsx): -->
<!-- logSummary, logProject, logTool, logEventType, logLinkUrl -->

<!-- Query keys: ['content-reviews'], ['activity-log', filters] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useCreateContent and useLogActivity mutation hooks</name>
  <files>src/hooks/useContentReviews.ts, src/hooks/useActivityLog.ts</files>
  <action>
In src/hooks/useContentReviews.ts, add a new exported function `useCreateContent()` following the exact same pattern as `useUpdateContentStatus`:
- Import useQueryClient and useMutation (already imported)
- mutationFn accepts `{ post_title: string, caption: string | null, content_type: ContentType, scheduled_date: string | null, day_label: string, week_number: number | null, platforms: string[], series: string | null, notes: string | null }`
- Guard with `if (!isSupabaseConfigured) return`
- Insert into `content_reviews` table with: post_title, caption, content_type, scheduled_date, day_label, week_number (default 0 if null), platforms, series, notes, plus defaults: status='draft', revision=1, slide_count=0, export_paths=[], excalidraw_paths=[]
- Use the same `(supabase as any)` cast pattern as useUpdateContentStatus to avoid strict type issues
- onSuccess: invalidate queryKey ['content-reviews'], show toast.success('Content created as draft')
- onError: toast.error('Failed to create content', { description: error.message })

In src/hooks/useActivityLog.ts, add a new exported function `useLogActivity()`:
- Add imports: `useMutation, useQueryClient` from '@tanstack/react-query', `toast` from 'sonner'
- mutationFn accepts `{ session_type: SessionType, project: string | null, tool: string | null, summary: string, metadata: Record<string, unknown> | null }`
- Guard with `if (!isSupabaseConfigured) return`
- Insert into `activity_log` table with all provided fields
- Use the same `(supabase as any)` cast pattern
- onSuccess: invalidate queryKey ['activity-log'] (no second element -- invalidates all variants), show toast.success('Activity logged')
- onError: toast.error('Failed to log activity', { description: error.message })
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Both mutation hooks export correctly and TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire mutations into ContentPipeline and ActivityLog page handlers</name>
  <files>src/pages/ContentPipeline.tsx, src/pages/ActivityLog.tsx</files>
  <action>
In src/pages/ContentPipeline.tsx -- AddContentDialog component:
- Add `useCreateContent` to the existing import from '@/hooks/useContentReviews'
- Inside AddContentDialog, call `const createContent = useCreateContent()`
- Replace the handleCreate function body: call `createContent.mutate({ post_title: newContent.title, caption: newContent.body || null, content_type: newContent.content_type, scheduled_date: newContent.scheduledDate || null, day_label: newContent.dayLabel, week_number: weekNum, platforms: newContent.platforms, series: newContent.series || null, notes: newContent.notes || null }, { onSuccess: () => { setNewContent(initialNewContent); onOpenChange(false) } })`
- Remove the existing toast.success call from handleCreate (it is now in the hook's onSuccess)
- On the Create button, add `disabled={!newContent.title.trim() || createContent.isPending}` (add isPending check)

In src/pages/ActivityLog.tsx:
- Add `import { useLogActivity } from '@/hooks/useActivityLog'` -- note: useActivityLog is already imported, but useLogActivity is a separate named export. Update the import to: `import { useActivityLog, useLogActivity } from '@/hooks/useActivityLog'`
- Inside the ActivityLog component, call `const logActivity = useLogActivity()`
- Replace handleLogSubmit body: call `logActivity.mutate({ session_type: logEventType as SessionType, project: logProject || null, tool: logTool, summary: logSummary, metadata: logLinkUrl ? { link_url: logLinkUrl } : null }, { onSuccess: () => { setShowLogForm(false); setLogSummary(''); setLogProject(''); setLogTool('manual'); setLogEventType('note'); setLogLinkUrl('') } })`
- On the Save button, add `disabled={!logSummary.trim() || logActivity.isPending}` (add isPending check)
- Add `import type { SessionType } from '@/types/database'` if not already imported (check existing imports -- it is imported on line 37 alongside ActivityEntry)
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Both forms persist data to Supabase on submit, show success toasts, reset form state, and close. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
npm run build passes with zero errors. Both mutations are exported and consumed.
</verification>

<success_criteria>
- Add Content modal inserts row into content_reviews and closes with toast
- Log Activity form inserts row into activity_log and closes with toast
- Query caches invalidate after insert so lists refresh
- npm run build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260323-16z-wire-add-content-and-log-activity-form-m/260323-16z-SUMMARY.md`

Then run:
```bash
git add src/hooks/useContentReviews.ts src/hooks/useActivityLog.ts src/pages/ContentPipeline.tsx src/pages/ActivityLog.tsx
git commit -m "fix: wire Add Content and Log Activity form mutations"
git push origin main
```
</output>
