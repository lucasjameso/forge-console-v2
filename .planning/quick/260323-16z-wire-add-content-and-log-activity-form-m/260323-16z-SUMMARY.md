---
phase: quick
plan: 260323-16z
subsystem: data-mutations
tags: [supabase, react-query, mutations, forms]
key-files:
  created: []
  modified:
    - src/hooks/useContentReviews.ts
    - src/hooks/useActivityLog.ts
    - src/pages/ContentPipeline.tsx
    - src/pages/ActivityLog.tsx
decisions: []
metrics:
  duration: 1min
  completed: "2026-03-23"
  tasks_completed: 2
  tasks_total: 2
---

# Quick Task 260323-16z: Wire Add Content and Log Activity Form Mutations

**One-liner:** useCreateContent and useLogActivity mutation hooks wired to Supabase with cache invalidation, toast feedback, and form reset on success.

## What Was Done

### Task 1: Add useCreateContent and useLogActivity mutation hooks

- Added `useCreateContent()` to `src/hooks/useContentReviews.ts` following the existing `useUpdateContentStatus` pattern
- Added `useLogActivity()` to `src/hooks/useActivityLog.ts` with the same mutation pattern
- Both hooks guard with `isSupabaseConfigured`, use `(supabase as any)` cast, invalidate query caches on success, and show toast notifications

### Task 2: Wire mutations into ContentPipeline and ActivityLog page handlers

- Updated `AddContentDialog` in ContentPipeline.tsx to call `createContent.mutate()` instead of a local toast
- Updated `handleLogSubmit` in ActivityLog.tsx to call `logActivity.mutate()` instead of just resetting state
- Both Save/Create buttons now include `isPending` in their disabled checks
- Form state resets and dialog closes happen in the `onSuccess` callback, ensuring data persisted before UI updates

## Commits

| Hash | Message |
|------|---------|
| 0868974 | fix: wire Add Content and Log Activity form mutations |

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None -- both mutations are fully wired to Supabase.

## Verification

- `npm run build` passes with zero errors
- TypeScript compiles cleanly (`npx tsc --noEmit` clean)
- Both mutation hooks exported and consumed by their respective pages

## Self-Check: PASSED
