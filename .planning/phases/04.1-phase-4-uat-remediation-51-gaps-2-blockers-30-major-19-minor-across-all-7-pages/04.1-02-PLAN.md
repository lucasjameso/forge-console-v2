---
phase: 04.1-phase-4-uat-remediation
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/components/dashboard/StatTilesRow.tsx
  - src/components/dashboard/ActionItemsCard.tsx
  - src/components/dashboard/ContentCalendarStrip.tsx
  - src/pages/Dashboard.tsx
autonomous: true
requirements:
  - UAT-DASH-STAT-TILES-GRID
  - UAT-DASH-STAT-TILES-LEFT-BORDER
  - UAT-DASH-CLARITY-PULSE
  - UAT-DASH-ACTION-ITEMS-INTERACTIVE
  - UAT-DASH-ACTION-ITEMS-COMPACT
  - UAT-DASH-CALENDAR-HEIGHT
  - UAT-DASH-CALENDAR-TYPE
  - UAT-DASH-GREETING-INSIGHTS
must_haves:
  truths:
    - "Stat tiles use CSS grid with repeat(5, 1fr) and maintain equal sizing across viewports"
    - "Stat tile recency borders are on the left edge, not top edge"
    - "CLARITY countdown has pulsing animation wired for <14 days"
    - "Action items render as compact clickable list with subtle separators"
    - "Content calendar cards are taller showing full titles and content type"
    - "Greeting subtitle rotates through contextual insights dynamically"
  artifacts:
    - path: "src/components/dashboard/StatTilesRow.tsx"
      provides: "CSS grid stat tiles with left border accents"
      contains: "grid-cols-5"
    - path: "src/components/dashboard/ActionItemsCard.tsx"
      provides: "Compact interactive action items list"
      contains: "onClick"
    - path: "src/components/dashboard/ContentCalendarStrip.tsx"
      provides: "Taller calendar cards with content type"
  key_links:
    - from: "src/components/dashboard/ActionItemsCard.tsx"
      to: "Dialog or detail view"
      via: "onClick handler on each item"
      pattern: "onClick"
---

<objective>
Fix all 8 Dashboard UAT gaps: stat tiles grid/borders, action items interactivity and layout, content calendar sizing, and dynamic greeting.

Purpose: Dashboard is the command center landing page. Every gap here affects daily experience.
Output: Rebuilt stat tiles row, compact action items, taller calendar cards, dynamic greeting.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-phase-4-uat-remediation-51-gaps-2-blockers-30-major-19-minor-across-all-7-pages/04.1-CONTEXT.md
@.planning/phases/04.1-phase-4-uat-remediation-51-gaps-2-blockers-30-major-19-minor-across-all-7-pages/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stat tiles (CSS grid, left borders, CLARITY pulse) and dynamic greeting</name>
  <files>src/components/dashboard/StatTilesRow.tsx, src/pages/Dashboard.tsx</files>
  <read_first>src/components/dashboard/StatTilesRow.tsx, src/pages/Dashboard.tsx, src/lib/colors.ts</read_first>
  <action>
**StatTilesRow.tsx -- CSS Grid + Left Borders + CLARITY Pulse:**

1. Replace the container's flexbox layout with CSS grid:
   - Container: `className="grid grid-cols-2 md:grid-cols-5 gap-4"`
   - This ensures all 5 tiles (including CLARITY Launch) are equal `1fr` width on desktop, 2 columns on mobile.

2. Move recency colored borders from top edge to left edge:
   - Change `border-t-4` (or `border-t-2`) to `border-l-4` on each tile card.
   - The color classes stay the same, just the border position changes.

3. CLARITY countdown pulsing animation:
   - Find the CLARITY Launch tile rendering. It currently shows amber treatment for 14-30 days.
   - Add conditional: when days remaining < 14, apply `animate-pulse` class to the tile AND change border color to `border-l-[hsl(var(--status-error))]`.
   - When days are 14-30: amber border, no pulse (static).
   - When days > 30: normal neutral border.

**Dashboard.tsx -- Dynamic Greeting Subtitle:**

4. Replace the static greeting subtitle with a dynamic insight rotator:
   - Create an `insights` array that pulls from available data (projects, content, action items):
     ```
     const insights = [
       { priority: daysUntilLaunch < 30 ? 1 : 5, text: `CLARITY launches in ${daysUntilLaunch} days` },
       { priority: staleProject ? 2 : 6, text: `${staleName} has had no activity in ${staleDays} days` },
       { priority: pendingApprovals > 0 ? 3 : 7, text: `${pendingApprovals} content pieces need approval` },
       { priority: highPriorityCount > 0 ? 4 : 8, text: `${highPriorityCount} high-priority items across projects` },
     ]
     ```
   - Sort by priority, display the most urgent one.
   - Use `useState` + `useEffect` with a 10-second interval to rotate through the top 3 insights with a fade transition (Framer Motion `AnimatePresence`).
   - If data hooks are not available for all insights, use what IS available (action items count, project data from useProjects).
  </action>
  <verify>
    <automated>grep -n "grid-cols-5\|grid-cols-2" src/components/dashboard/StatTilesRow.tsx && grep -n "border-l-" src/components/dashboard/StatTilesRow.tsx | head -5 && grep -n "animate-pulse" src/components/dashboard/StatTilesRow.tsx && grep -n "insights\|priority.*text" src/pages/Dashboard.tsx | head -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- StatTilesRow.tsx contains `grid-cols-5` (not flex)
- StatTilesRow.tsx contains `grid-cols-2` for mobile breakpoint
- StatTilesRow.tsx contains `border-l-4` (not border-t-)
- StatTilesRow.tsx contains `animate-pulse` conditional for CLARITY tile <14 days
- Dashboard.tsx contains an insights array with priority-sorted contextual messages
- Zero instances of `border-t-4` or `border-t-2` remain in StatTilesRow.tsx for tile accents
- `npm run build` exits 0
  </acceptance_criteria>
  <done>Stat tiles use equal CSS grid columns with left accent borders. CLARITY pulse wired for <14 days. Greeting rotates through contextual insights.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild action items as compact interactive list and enhance calendar cards</name>
  <files>src/components/dashboard/ActionItemsCard.tsx, src/components/dashboard/ContentCalendarStrip.tsx</files>
  <read_first>src/components/dashboard/ActionItemsCard.tsx, src/components/dashboard/ContentCalendarStrip.tsx, src/hooks/useProjects.ts</read_first>
  <action>
**ActionItemsCard.tsx -- Compact Interactive List:**

1. Replace the current card-based layout with a compact list using `divide-y`:
   ```tsx
   <div className="divide-y divide-[hsl(var(--border-subtle))]">
     {items.map(item => (
       <button key={item.id} onClick={() => setSelectedItem(item)}
         className="w-full flex items-center gap-3 py-2.5 px-3 hover:bg-[hsl(var(--bg-elevated))] text-left transition-colors">
         <PriorityDot priority={item.priority} />
         <span className="flex-1 text-sm truncate">{item.title}</span>
         <Badge variant="outline" className="text-xs">{item.project}</Badge>
         <span className="text-xs text-[hsl(var(--text-tertiary))]">{item.due || 'No due date'}</span>
       </button>
     ))}
   </div>
   ```

2. Add a detail modal/sheet using shadcn Dialog that opens when an item is clicked:
   - Show: item title, description, project, priority, due date
   - Action buttons: "Mark Complete" (calls mutation or shows toast), "Snooze" (shows toast), "Add Note" (textarea + save)
   - Use `useState` for `selectedItem` to control dialog open/close.
   - Import Dialog from `@/components/ui/dialog`.

3. The PriorityDot component: a small colored circle (w-2 h-2 rounded-full) using status colors:
   - high/urgent: `bg-[hsl(var(--status-error))]`
   - medium: `bg-[hsl(var(--status-warning))]`
   - low: `bg-[hsl(var(--status-success))]`

4. Keep the "Show All / Show Less" toggle for expanding beyond 5 items.

**ContentCalendarStrip.tsx -- Taller Cards with Content Type:**

5. Increase the min-height of content preview cards. Change from whatever compact height exists to `min-h-[80px]` or similar so titles don't truncate.

6. Remove or relax `truncate`/`line-clamp-1` on the title text. Use `line-clamp-2` instead to show more of the title.

7. Below the title, add a content type label:
   ```tsx
   <span className="text-[11px] text-[hsl(var(--text-tertiary))] capitalize">
     {item.content_type || item.type || 'Post'}
   </span>
   ```
   Pull from whichever field on the content item holds the type (check the type definition). If no field exists, default to "Post".
  </action>
  <verify>
    <automated>grep -n "divide-y\|onClick.*setSelected\|Dialog" src/components/dashboard/ActionItemsCard.tsx | head -10 && grep -n "min-h-\|content_type\|line-clamp-2" src/components/dashboard/ContentCalendarStrip.tsx | head -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- ActionItemsCard.tsx contains `divide-y` (compact list, not cards)
- ActionItemsCard.tsx contains `onClick` handler on each item row
- ActionItemsCard.tsx contains `Dialog` import for detail modal
- ActionItemsCard.tsx contains PriorityDot with `status-error`, `status-warning`, `status-success` colors
- ContentCalendarStrip.tsx contains `min-h-[80px]` or similar increased height
- ContentCalendarStrip.tsx contains `line-clamp-2` (not line-clamp-1)
- ContentCalendarStrip.tsx contains content type display text
- `npm run build` exits 0
  </acceptance_criteria>
  <done>Action items render as compact clickable list with detail dialog. Calendar cards show full titles and content type below.</done>
</task>

</tasks>

<verification>
- Dashboard stat tiles are equal width CSS grid with left accent borders
- CLARITY tile has pulse animation conditional
- Action items are compact list rows with click-to-open detail dialog
- Content calendar cards are taller with content type shown
- Greeting rotates through contextual insights
- `npm run build` passes
</verification>

<success_criteria>
All 8 Dashboard UAT gaps resolved. Stat tiles responsive and equal. Action items interactive and compact. Calendar cards informative. Greeting dynamic.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-phase-4-uat-remediation-51-gaps-2-blockers-30-major-19-minor-across-all-7-pages/04.1-02-SUMMARY.md`
</output>
