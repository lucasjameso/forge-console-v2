---
phase: 04.1-phase-4-uat-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - UAT-GLOBAL-BORDER-RADIUS
  - UAT-DEPS
must_haves:
  truths:
    - "Border radius on all cards, tiles, badges, containers, input areas, pills, and modals is approximately half the previous value"
    - "New dependencies @dnd-kit/core, @dnd-kit/sortable, and remark-gfm are installed and importable"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Halved border-radius CSS variables"
      contains: "--radius: 0.3125rem"
    - path: "package.json"
      provides: "New dependencies for kanban and markdown"
      contains: "@dnd-kit/core"
  key_links: []
---

<objective>
Fix the global border-radius CSS variables to half their current values and install new dependencies needed by downstream plans.

Purpose: The border-radius fix affects every page in the app and was reported on 6/7 UAT tests. Installing deps in Wave 1 unblocks downstream plans that need @dnd-kit and remark-gfm.
Output: Updated globals.css with halved radius values, new npm packages installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-phase-4-uat-remediation-51-gaps-2-blockers-30-major-19-minor-across-all-7-pages/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Halve all border-radius CSS variables globally</name>
  <files>src/styles/globals.css</files>
  <read_first>src/styles/globals.css</read_first>
  <action>
In `src/styles/globals.css`, find the radius variables in the `:root` block and update them to these exact values:

```
--radius: 0.3125rem;     /* was 0.625rem (10px -> 5px) */
--radius-sm: 3px;         /* was 6px */
--radius-md: 5px;         /* was 10px */
--radius-lg: 7px;         /* was 14px */
--radius-xl: 9px;         /* was 18px */
/* --radius-pill stays at 100px -- pills should remain fully round */
```

After updating the variables, grep the entire `src/` directory for hardcoded `rounded-[` values (e.g., `rounded-[14px]`, `rounded-[18px]`). Any found should be converted to use the token system (`rounded-sm`, `rounded-md`, `rounded-lg`, `rounded-xl`). Document which files had hardcoded values in the summary.

Do NOT change `--radius-pill: 100px` -- that stays the same for fully-round pill shapes.
  </action>
  <verify>
    <automated>grep -n "radius" src/styles/globals.css | head -10 && echo "---" && grep -rn "rounded-\[" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | head -20</automated>
  </verify>
  <acceptance_criteria>
- src/styles/globals.css contains `--radius: 0.3125rem`
- src/styles/globals.css contains `--radius-sm: 3px`
- src/styles/globals.css contains `--radius-md: 5px`
- src/styles/globals.css contains `--radius-lg: 7px`
- src/styles/globals.css contains `--radius-xl: 9px`
- src/styles/globals.css contains `--radius-pill: 100px` (unchanged)
- Zero instances of `rounded-[14px]` or `rounded-[18px]` remain in src/ (converted to tokens)
  </acceptance_criteria>
  <done>All border-radius values halved globally. No hardcoded rounded pixel values remain.</done>
</task>

<task type="auto">
  <name>Task 2: Install new dependencies for downstream plans</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json</read_first>
  <action>
Run: `npm install @dnd-kit/core @dnd-kit/sortable remark-gfm`

These are needed by:
- @dnd-kit/core + @dnd-kit/sortable: Content Pipeline Kanban drag-and-drop (Plan 07)
- remark-gfm: Settings feedback markdown rendering (Plan 08)

After install, verify the build still passes with `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- package.json contains `"@dnd-kit/core"` in dependencies
- package.json contains `"@dnd-kit/sortable"` in dependencies
- package.json contains `"remark-gfm"` in dependencies
- `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All three new dependencies installed. Build passes cleanly.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- CSS radius variables are at halved values
- New packages appear in package.json dependencies
</verification>

<success_criteria>
Border radius is globally halved across the entire app. New dependencies are available for downstream plans. Build compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-phase-4-uat-remediation-51-gaps-2-blockers-30-major-19-minor-across-all-7-pages/04.1-01-SUMMARY.md`
</output>
