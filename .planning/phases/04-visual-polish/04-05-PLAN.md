---
phase: 04-visual-polish
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/SocialMedia.tsx
  - src/pages/Settings.tsx
  - src/hooks/usePageFeedback.ts
  - src/hooks/useSocialPlatforms.ts
autonomous: true
requirements:
  - VISL-09
  - VISL-11
  - SFIX-01
  - SFIX-02
  - SFIX-03
  - SFIX-04
  - SFIX-05
  - SFIX-06
  - SFIX-07
  - SFIX-08
  - SFIX-09
  - SFIX-10
  - SFIX-11
  - SFIX-12
  - STFIX-01
  - STFIX-02
  - STFIX-03
  - STFIX-04
  - STFIX-05
  - STFIX-06
  - STFIX-07
  - STFIX-08
  - STFIX-09
  - STFIX-10
  - STFIX-11
  - STFIX-12
  - D-15

must_haves:
  truths:
    - "Social Media platform cards show real brand icons not generic Lucide icons"
    - "Active platforms have larger cards, setup platforms are compact in 3-col grid"
    - "Stats are at the top as a hero row"
    - "LinkedIn shows follower goal progress with percentage and green bar"
    - "Settings integration cards show brand logos and human-readable labels"
    - "Feedback entries are compact with markdown rendering and expand toggle"
    - "Connected/disconnected integrations have green/red visual distinction"
    - "Test connection buttons exist on integration cards"
  artifacts:
    - path: "src/pages/SocialMedia.tsx"
      provides: "Social media page with brand icons and adaptive layout"
      contains: "PLATFORM_ICONS"
    - path: "src/pages/Settings.tsx"
      provides: "Settings page with brand logos, organized sections, feedback rendering"
      contains: "INTEGRATION_ICONS"
  key_links:
    - from: "src/pages/SocialMedia.tsx"
      to: "src/lib/icons.ts"
      via: "import PLATFORM_ICONS"
      pattern: "import.*PLATFORM_ICONS"
    - from: "src/pages/Settings.tsx"
      to: "src/lib/icons.ts"
      via: "import INTEGRATION_ICONS"
      pattern: "import.*INTEGRATION_ICONS"
---

<objective>
Fix all 12 Social Media issues (SFIX-01 through SFIX-12) and all 12 Settings issues (STFIX-01 through STFIX-12), plus page polish (VISL-09, VISL-11).

Purpose: Both pages share the brand icon dependency. Social Media needs adaptive layout with real icons and importance-based sizing. Settings needs organized sections, brand logos, feedback markdown rendering, connection health, and human-readable labels.

Output: SocialMedia.tsx and Settings.tsx fully polished with all 24 fixes applied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/feedback/PHASE-04-FIXES-SPEC.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md

@src/lib/icons.ts
@src/lib/colors.ts
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Social Media page -- brand icons, adaptive layout, stat row, and all SFIX items</name>
  <files>src/pages/SocialMedia.tsx, src/hooks/useSocialPlatforms.ts</files>
  <read_first>
    src/pages/SocialMedia.tsx, src/hooks/useSocialPlatforms.ts, src/data/mock.ts,
    src/lib/icons.ts, src/components/layout/PageShell.tsx
  </read_first>
  <action>
    **SFIX-01: Real brand icons**
    - Import `PLATFORM_ICONS` from `@/lib/icons`
    - For each platform card, look up the icon: `const Icon = PLATFORM_ICONS[platform.platform_name?.toLowerCase()]`
    - If icon found, render `<Icon size={24} />` (or appropriate size)
    - If icon NOT found (e.g., beehiiv, goodreads, gumroad), fall back to the existing Lucide icon or a generic Globe icon
    - Remove ALL generic Lucide icon usage for platforms that have brand icons

    **SFIX-02: Cards sized by importance**
    - Split platforms into two groups: "Active Platforms" (those with `status === 'active'` or `followers > 0`) and "Needs Setup" (rest)
    - Active section: `grid grid-cols-1 md:grid-cols-2 gap-5` (larger cards)
    - Setup section: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` (compact cards, shorter)
    - Section headers: "Active Platforms" and "Needs Setup" in 18px font-semibold with appropriate sub-count

    **SFIX-03: Setup banner replaced with badge + amber border**
    - Remove any full-width amber "This platform needs to be set up" banner
    - Replace with: `<Badge variant="warning">Setup Needed</Badge>` in top-right of card
    - Add 4px amber left border: `border-l-4 border-l-[hsl(var(--status-warning))]`

    **SFIX-04: Unset platforms minimal content**
    - For setup-needed platforms, show ONLY: brand icon + platform name + handle (if known) + "Setup Needed" badge
    - Remove "No followers yet", "Needs profile link", follower count of 0
    - Details appear on click/expand only (or just remove -- no expand needed for Phase 4)

    **SFIX-05: Stats as hero row at top**
    - Move overview stats to TOP, below PageShell header, above platform sections
    - Render as stat tiles row (same pattern as Dashboard stat tiles):
      - "Active Platforms" -- count, green badge if > 0
      - "Total Followers" -- large number formatted with commas
      - "Need Setup" -- count, amber if > 0
    - Style: flex row with gap-4, each tile in a card with p-4, rounded-lg, border

    **SFIX-06: LinkedIn follower goal visual**
    - On the LinkedIn card specifically (match by platform_name === 'linkedin' and type === 'personal' or similar):
    - Show: current followers / goal (e.g., "6,550 / 10,000")
    - Percentage: `Math.round((current / goal) * 100)` + "%" in bold
    - Green progress bar: `<div className="h-2 rounded-full bg-[hsl(var(--bg-elevated))]"><div className="h-2 rounded-full bg-[hsl(var(--status-success))]" style={{ width: `${percentage}%` }} /></div>`
    - If goal data not in mock/Supabase, hardcode goal as 10000 for LinkedIn

    **SFIX-07: Card content density standardization**
    - Active cards: icon + name + handle + follower count + last post date + profile link icon + goal (if applicable)
    - Setup cards: icon + name + handle + badge. Nothing else.

    **SFIX-08: CLARITY launch badges**
    - For platforms needed for CLARITY launch (Goodreads, Amazon Author Central, BookBub), add: `<Badge variant="coral">Needed for launch</Badge>`
    - Identify these platforms by name match or add a `launch_critical` flag in the data

    **SFIX-09: External link icons**
    - Every card with a known profile URL: small ExternalLink icon button (14px) in card top-right corner
    - No URL: show nothing. Remove any "View profile" text links

    **SFIX-10: Dynamic page subtitle**
    - Replace static subtitle with computed string from platform data:
      - `"{activeCount} active, {setupCount} need setup. {totalFollowers.toLocaleString()} total followers."`
    - Pass as subtitle to PageShell

    **SFIX-11: Adaptive grid layout**
    - Remove any `<style>` tag injections (D-13) for grid layout
    - Replace with Tailwind responsive grid classes (already done in SFIX-02 above)
    - Ensure the grid adapts: 1 col on mobile, 2 col for active on md+, 3 col for setup on lg+

    **SFIX-12: Sort dropdown**
    - Add a Select dropdown in the page header area: "Sort by: Priority | Alphabetical | Followers | Last active"
    - State: `const [sortBy, setSortBy] = useState<'priority' | 'alpha' | 'followers' | 'active'>('priority')`
    - Sort logic:
      - priority: active first, then launch-critical, then alphabetical
      - alpha: by platform name
      - followers: descending by follower count
      - active: by last_active date descending
    - Use shadcn Select component

    **Inline style cleanup (D-12, D-13):** Remove ALL `style={{}}` objects. Remove the `<style>` tag at line ~242. Replace with Tailwind classes.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/SocialMedia.tsx contains "PLATFORM_ICONS" import
    - src/pages/SocialMedia.tsx contains "Active Platforms" section header text
    - src/pages/SocialMedia.tsx contains "Needs Setup" or "Need Setup" section
    - src/pages/SocialMedia.tsx contains "lg:grid-cols-3" (compact grid for setup cards)
    - src/pages/SocialMedia.tsx contains "Setup Needed" badge text
    - src/pages/SocialMedia.tsx contains "Needed for launch" badge text
    - src/pages/SocialMedia.tsx contains "sortBy" state variable
    - src/pages/SocialMedia.tsx does NOT contain "<style>" tag
    - npm run build exits 0
  </acceptance_criteria>
  <done>All 12 SFIX items implemented: brand icons on all platforms, adaptive grid, stat hero row, LinkedIn goal visual, CLARITY launch badges, sort dropdown, setup cards minimal, external links standardized.</done>
</task>

<task type="auto">
  <name>Task 2: Settings page -- brand logos, feedback rendering, organized sections, connection health, all STFIX items</name>
  <files>src/pages/Settings.tsx, src/hooks/usePageFeedback.ts</files>
  <read_first>
    src/pages/Settings.tsx, src/hooks/usePageFeedback.ts, src/hooks/useSystemHealth.ts,
    src/lib/icons.ts, src/lib/colors.ts, src/types/database.ts
  </read_first>
  <action>
    **STFIX-01: Integration brand logos**
    - Import `INTEGRATION_ICONS` from `@/lib/icons`
    - For each integration card, look up icon: `const Icon = INTEGRATION_ICONS[service.toLowerCase()]`
    - Render `<Icon size={24} />` if found, fall back to Lucide icon if not (e.g., n8n may not have icon)

    **STFIX-02: Feedback markdown rendering**
    - Import `ReactMarkdown` from `react-markdown`
    - Render feedback content through `<ReactMarkdown>{entry.content}</ReactMarkdown>` instead of raw text
    - Wrap in a `prose prose-sm` container for proper formatting (or custom Tailwind classes if prose not available)
    - Truncate to 3-4 lines when collapsed: `line-clamp-3` on the container

    **STFIX-03: Compact feedback entries**
    - Default collapsed state: each entry is a single row ~48-56px:
      - Left: type icon (wrench for fix, lightbulb for suggestion per STFIX-11)
      - Middle: page badge (colored per STFIX-12) + first line as title (truncated) + status badge
      - Right: timestamp in 12px text-tertiary
    - Click to expand: full content with markdown rendering
    - State: `const [expandedId, setExpandedId] = useState<string | null>(null)`
    - Target: 8-10 entries visible per screen without scrolling

    **STFIX-04: Human-readable integration labels**
    - Replace env var display (like `VITE_SUPABASE_URL`) with human-readable labels:
      - `VITE_SUPABASE_URL` -> "Database URL"
      - `VITE_SUPABASE_ANON_KEY` -> "API Key"
      - `VITE_ANTHROPIC_API_KEY` -> "API Key"
      - etc.
    - Create a label map: `const ENV_LABELS: Record<string, string> = { ... }`
    - Show original env var name in a tooltip on hover (for debugging)

    **STFIX-05: Connected/disconnected visual distinction**
    - Connected: green left border (`border-l-4 border-l-[hsl(var(--status-success))]`), `<Badge variant="success">Connected</Badge>`, green check icon
    - Disconnected: red left border (`border-l-4 border-l-[hsl(var(--status-error))]`), `<Badge variant="error">Disconnected</Badge>`, red X icon
    - Partial: amber border + warning badge
    - Determine status: check if env var is set (non-empty) = connected, else disconnected

    **STFIX-06: Dashboard links per integration**
    - Add external links at bottom of each integration card:
      - Supabase: `https://supabase.com/dashboard`
      - n8n: local URL or `https://app.n8n.io`
      - Cloudflare: `https://dash.cloudflare.com`
      - Slack: `https://slack.com/workspace`
      - Anthropic: `https://console.anthropic.com`
    - Style: coral text + ExternalLink icon, 13px, bottom-left of card

    **STFIX-07: About/System Info section enhanced**
    - Move to a "System Info" section (or tab)
    - Display: app name, version (from package.json or hardcoded "v2.0"), build date (current date as fallback), environment (development/production), database name, table count (from system_health if available), GitHub repo link

    **STFIX-08: Organized sections**
    - Use shadcn Tabs component to organize Settings into tabs:
      - "Integrations" (default) -- integration cards
      - "Feedback" -- feedback log with filters
      - "System" -- about/system info section
    - Each tab renders its own section content
    - Placeholder tabs for Phase 9: "Preferences", "Data Management" (show "Coming soon" message)

    **STFIX-09: Test connection buttons**
    - Each integration card gets a "Test" button (ghost variant, small)
    - On click, attempt a basic health check:
      - Supabase: `supabase.from('projects').select('id').limit(1)` -- if succeeds, show "Healthy, {ms}ms"
      - Anthropic: simple fetch to verify API key format (or skip if no safe endpoint)
      - Others: basic URL reachability test or just show "Test not available"
    - Result display: green "Healthy, 120ms" or red "Failed: timeout" text below button
    - State: `const [testResults, setTestResults] = useState<Record<string, { status: string; ms?: number }>>({})`

    **STFIX-10: Filter tabs with count badges**
    - Feedback filter tabs: "All (N)", "Open (N)", "Done (N)"
    - "Open" should be the default active tab
    - Counts computed from feedback data
    - If 0 open items: show "All feedback has been addressed" message in a subtle green box

    **STFIX-11: Fix vs suggestion distinction**
    - Fix entries: Wrench icon (Lucide `Wrench`), amber/red left border
    - Suggestion entries: Lightbulb icon (Lucide `Lightbulb`), blue/navy left border
    - Determine from entry `type` field: 'fix' vs 'suggestion'

    **STFIX-12: Page-colored badges on feedback entries**
    - Each feedback entry shows which page it came from as a colored badge pill
    - Colors per D-11: Dashboard=coral, Projects=navy, Brain Dump=purple, Content Pipeline=green, Social Media=blue, Activity Log=amber, Settings=gray
    - Import `getPageColorVar` from `@/lib/colors`
    - Render as small badge with dynamic color: `style={{ color: getPageColorVar(entry.page), backgroundColor: ... }}`

    **D-15: Fix usePageFeedback.ts type cast**
    - In `usePageFeedback.ts`, find the `as ReturnType<typeof supabase.from>` cast
    - Replace with proper typing now that `page_feedback` is defined in `database.ts`
    - Should be: `supabase.from('page_feedback')` -- TypeScript should now recognize it

    **Inline style cleanup (D-12, D-13):** Remove ALL `style={{}}` objects. Remove `<style>` tag at line ~368 in Settings.tsx. Replace with Tailwind.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Settings.tsx contains "INTEGRATION_ICONS" import
    - src/pages/Settings.tsx contains "ReactMarkdown" or "react-markdown" import
    - src/pages/Settings.tsx contains "Connected" and "Disconnected" badge text
    - src/pages/Settings.tsx contains "Test" button text or "testResults" state
    - src/pages/Settings.tsx contains "Integrations" and "Feedback" and "System" tab labels
    - src/pages/Settings.tsx does NOT contain "<style>" tag
    - src/pages/Settings.tsx contains "supabase.com/dashboard" or similar external link
    - src/hooks/usePageFeedback.ts does NOT contain "as ReturnType"
    - npm run build exits 0
  </acceptance_criteria>
  <done>All 12 STFIX items implemented: brand logos, markdown feedback rendering, compact entries, human-readable labels, connection health visuals, test buttons, organized tabs, page-colored badges, fix vs suggestion distinction.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Social Media shows brand icons, adaptive grid, hero stats, sort dropdown
- Settings shows brand logos, organized tabs, compact feedback, test buttons
- No `<style>` tags remain in either file
- No excessive inline styles remain
</verification>

<success_criteria>
- All 12 SFIX items + all 12 STFIX items resolved (24 total)
- VISL-09 (SocialMedia) and VISL-11 (Settings) polished
- D-15 type cast fix applied
- All inline styles converted, style tags removed (D-12, D-13)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-05-SUMMARY.md`
</output>
