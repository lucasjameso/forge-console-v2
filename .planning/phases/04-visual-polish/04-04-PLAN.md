---
phase: 04-visual-polish
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/ContentPipeline.tsx
  - src/hooks/useContentReviews.ts
autonomous: true
requirements:
  - VISL-08
  - CFIX-01
  - CFIX-02
  - CFIX-03
  - CFIX-04
  - CFIX-05
  - CFIX-06
  - CFIX-07
  - CFIX-08
  - CFIX-09
  - CFIX-10
  - CFIX-11
  - CFIX-12

must_haves:
  truths:
    - "Month view renders as a real calendar grid with date numbers on every cell"
    - "Navigation arrows allow browsing forward/back through months"
    - "Today has a visual indicator in all 4 views"
    - "List view cards are compact at 80-90px height"
    - "Kanban empty columns show proper empty state"
    - "Status badges are standardized across all views (pill shape, consistent colors)"
    - "Text posts show Text post instead of 0 slides"
    - "Add Content button appears in page header"
  artifacts:
    - path: "src/pages/ContentPipeline.tsx"
      provides: "Complete content pipeline with all 12 fixes across 4 view modes"
      contains: "StatusBadge"
  key_links:
    - from: "src/pages/ContentPipeline.tsx"
      to: "src/components/ui/StatusBadge.tsx"
      via: "import StatusBadge"
      pattern: "import.*StatusBadge"
---

<objective>
Fix all 12 Content Pipeline issues (CFIX-01 through CFIX-12) and apply overall page polish (VISL-08).

Purpose: Content Pipeline is the most complex page (503 lines, 4 view modes). Calendar needs to look like a real calendar, navigation needs arrows, status badges need standardization, and several UX issues need fixing across month/week/list/kanban views.

Output: ContentPipeline.tsx fully polished with all 12 fixes applied across all 4 view modes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/feedback/PHASE-04-FIXES-SPEC.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md

@src/components/ui/StatusBadge.tsx
@src/lib/colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild month view as real calendar with navigation, fix list and week views (CFIX-01 through CFIX-05, CFIX-09 through CFIX-11)</name>
  <files>src/pages/ContentPipeline.tsx</files>
  <read_first>
    src/pages/ContentPipeline.tsx, src/hooks/useContentReviews.ts, src/data/mock.ts
  </read_first>
  <action>
    **CFIX-01: Month view as real calendar grid**
    - Replace existing month view with a proper CSS grid calendar:
      - 7 columns (Mon-Sun), column headers sticky at top: `grid grid-cols-7`
      - Column headers: day abbreviations (Mon, Tue, ...) in 12px uppercase font-medium text-tertiary
      - Each day cell: minimum height 140px (`min-h-[140px]`), visible borders (`border border-[hsl(var(--border-subtle))]`)
      - Day number in top-left of every cell: 14px font-semibold, `text-[hsl(var(--text-primary))]`
      - Content items inside cells rendered as small pills: truncated title, status color dot
      - Use `date-fns` to calculate first day of month, pad with previous month days (grayed out)
      - State: `const [currentMonth, setCurrentMonth] = useState(new Date())`

    **CFIX-02: Empty day cells show dates**
    - Every cell MUST show the date number regardless of content
    - Empty cells: date number + dashed border bottom or subtle bg tint `bg-[hsl(var(--bg-elevated)/0.5)]`
    - Non-empty cells: date number + content items below

    **CFIX-03: Month navigation**
    - Add navigation controls in the view header area:
      - Left arrow button (ChevronLeft icon): `setCurrentMonth(prev => subMonths(prev, 1))`
      - Right arrow button (ChevronRight icon): `setCurrentMonth(prev => addMonths(prev, 1))`
      - Month/year label between arrows: `format(currentMonth, 'MMMM yyyy')` in 18px font-semibold
      - "Today" button: `setCurrentMonth(new Date())` -- ghost variant button
    - Add Framer Motion slide transition when month changes: `animate={{ x: direction === 'next' ? [20, 0] : [-20, 0], opacity: [0, 1] }}`
    - Import `subMonths`, `addMonths`, `startOfMonth`, `endOfMonth`, `eachDayOfInterval`, `startOfWeek`, `endOfWeek`, `isSameMonth` from `date-fns`

    **CFIX-04: List view compact cards**
    - Each list item card should be 80-90px height max
    - Layout: flex row with `items-center gap-4 p-4 h-[84px]`
    - Left side: week/day label + formatted date in 12px text-tertiary
    - Center: title (14px font-medium, truncate with `truncate` class), description (13px text-secondary, `line-clamp-1`)
    - Right side: StatusBadge, formatted date
    - Bottom metadata row: platform icon (small), slide count or "Text post"

    **CFIX-05: Week view stronger containers**
    - Each week group wrapped in a card: `rounded-lg border border-[hsl(var(--border-subtle))] p-4`
    - Week header bar: week number + date range ("Week 12: Mar 17 - Mar 23") in 15px font-semibold, with bg-elevated background, rounded-t-lg, p-3
    - 32px gap between week groups: `space-y-8`
    - Content items within each week as compact rows

    **CFIX-09: Standardized status badges**
    - Replace ALL existing status badge/pill/tag rendering across ALL 4 views with `<StatusBadge status={item.status} />` imported from `@/components/ui/StatusBadge`
    - This ensures consistent: 11px uppercase, font-semibold, px-2.5 py-0.5, rounded-full, Draft=gray, Pending=amber, Approved=green, Rejected=red, Posted=blue

    **CFIX-10: Today indicator in all views**
    - Import `isToday` from `date-fns`
    - Month view: today's cell gets `border-t-2 border-t-[hsl(var(--accent-coral))]` and subtle warm bg `bg-[hsl(var(--accent-coral)/0.04)]`
    - Week view: items scheduled for today get a small "Today" badge (coral, 10px)
    - List view: today's items get a warm bg tint on the row
    - Kanban view: items scheduled for today get a "Today" badge on the card

    **CFIX-11: "Text post" vs "0 slides"**
    - Find where slide count is rendered (likely showing `${item.slide_count} slides` or similar)
    - Logic:
      - If `item.content_type === 'text'` or `item.slide_count === null`: show "Text post" in 12px text-secondary
      - If `item.content_type === 'carousel'` and `item.slide_count === 0`: show "No slides yet" in 12px `text-[hsl(var(--status-warning))]`
      - If `item.slide_count > 0`: show `${item.slide_count} slides` with Layers icon

    **Inline style cleanup (D-12):** Convert ALL `style={{}}` objects to Tailwind. Remove any `<style>` tags.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ContentPipeline.tsx contains "grid-cols-7" (real calendar grid)
    - src/pages/ContentPipeline.tsx contains "currentMonth" or "setCurrentMonth" (navigation state)
    - src/pages/ContentPipeline.tsx contains "addMonths" or "subMonths" (month navigation)
    - src/pages/ContentPipeline.tsx contains "StatusBadge" (standardized badges)
    - src/pages/ContentPipeline.tsx contains "isToday" (today indicator)
    - src/pages/ContentPipeline.tsx contains "Text post" (CFIX-11 fix)
    - src/pages/ContentPipeline.tsx contains "line-clamp-1" or "truncate" (list view compact)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Month view is a real calendar with date grid, navigation works, list view compact, week view has containers, status badges standardized, today indicated, text posts labeled correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix kanban view, content detail modal, and add content button (CFIX-06 through CFIX-08, CFIX-12)</name>
  <files>src/pages/ContentPipeline.tsx</files>
  <read_first>
    src/pages/ContentPipeline.tsx, src/components/ui/dialog.tsx
  </read_first>
  <action>
    **CFIX-06: Kanban empty columns**
    - Empty kanban columns (no items in that status) should show:
      - Dashed border container: `border-2 border-dashed border-[hsl(var(--border-subtle))] rounded-lg p-8`
      - Centered text: "No items" in 13px `text-[hsl(var(--text-tertiary))]`
      - Subtle empty-state icon (e.g., Lucide `Inbox` at 24px, opacity-40)
    - Max-height on columns: `max-h-[calc(100vh-280px)] overflow-y-auto` so individual columns scroll when they have more than ~6 items
    - Column header: status name + count badge, 14px font-semibold

    **CFIX-07: Kanban card metadata**
    - Each kanban card should contain:
      - Title: 14px font-medium, max 2 lines with `line-clamp-2`
      - StatusBadge in top-right corner
      - Formatted date: "Mar 24" in 12px text-tertiary
      - Slide count with Layers icon OR "Text post" (reuse CFIX-11 logic)
      - Platform icon from `PLATFORM_ICONS` if platform data exists (small, 14px)
    - Card styling: `bg-[hsl(var(--bg-surface))] border border-[hsl(var(--border-subtle))] rounded-lg p-3 shadow-sm hover:shadow-md transition-shadow`

    **CFIX-08: Content detail modal**
    - Use shadcn Dialog component (`@/components/ui/dialog`)
    - Modal triggered when clicking any content item (in any view)
    - State: `const [selectedItem, setSelectedItem] = useState<ContentReview | null>(null)`
    - Modal content at 680px min-width: `<DialogContent className="sm:max-w-[680px]">`
    - Layout:
      - `<DialogHeader>`: Title (18px font-semibold), StatusBadge, formatted date
      - Caption in a textarea (readonly for now -- editing is Phase 6): `<Textarea value={item.caption} readOnly className="min-h-[120px]" />`
      - Metadata row: platform, slide count/text post, scheduled date
      - Action buttons at bottom:
        - If status === 'pending': "Approve" (green) and "Reject" (red outline) buttons
        - If status === 'rejected': "Re-submit" button (coral)
        - All buttons show toast on click and call appropriate mutation from `useContentReviews`
      - Close button via DialogClose

    **CFIX-12: "Add Content" button**
    - Add a button in the page header (next to view toggle buttons): "Add Content" with Plus icon, coral variant
    - On click, open a creation modal (Dialog):
      - Title input
      - Caption textarea
      - Week/day selector (simple number inputs or dropdown)
      - Platform checkboxes (LinkedIn, Medium, etc.)
      - Date picker (simple date input for now)
      - "Create as Draft" button that calls content creation mutation and shows toast
    - If creation mutation doesn't exist in `useContentReviews`, add a stub that inserts into content_reviews table or just shows toast "Content created" with mock behavior

    **Inline style cleanup (D-12):** Ensure ALL remaining `style={{}}` in ContentPipeline.tsx are converted. Remove any `<style>` tags (D-13 if present).
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ContentPipeline.tsx contains "border-dashed" (empty kanban columns)
    - src/pages/ContentPipeline.tsx contains "max-h-[calc" or "overflow-y-auto" (kanban column scroll)
    - src/pages/ContentPipeline.tsx contains "line-clamp-2" (kanban card title truncation)
    - src/pages/ContentPipeline.tsx contains "DialogContent" (detail modal)
    - src/pages/ContentPipeline.tsx contains "sm:max-w-[680px]" or "max-w-[680px]" (modal width)
    - src/pages/ContentPipeline.tsx contains "Add Content" (creation button)
    - src/pages/ContentPipeline.tsx contains "Create as Draft" (creation modal)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Kanban has proper empty states and card metadata, content detail modal opens at 680px with actions, Add Content button in header opens creation modal.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- All 4 views (month, week, list, kanban) render correctly
- Month view looks like a real calendar with navigation
- Status badges consistent across all views
- Detail modal opens on item click
- Add Content button visible in header
</verification>

<success_criteria>
- All 12 CFIX items resolved
- VISL-08 (ContentPipeline polish) achieved across all 4 view modes
- All inline styles converted to Tailwind (D-12)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-04-SUMMARY.md`
</output>
