---
phase: 04-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/BrainDump.tsx
  - src/hooks/useBrainDump.ts
autonomous: true
requirements:
  - VISL-07
  - BFIX-01
  - BFIX-02
  - BFIX-03
  - BFIX-04
  - BFIX-05
  - BFIX-06
  - BFIX-07
  - BFIX-08

must_haves:
  truths:
    - "Project selector pills appear above the textarea with project colors"
    - "Submit button shows keyboard shortcut hint and is full-width on mobile"
    - "History entries have project-colored left borders"
    - "Expanded entries show parsed output with visual formatting"
    - "Status progression is visible on each entry"
    - "Textarea starts at 120px and auto-grows"
    - "Processing state shows skeleton shimmer with Parsing label"
    - "History is grouped by day with Today/Yesterday/date headers"
  artifacts:
    - path: "src/pages/BrainDump.tsx"
      provides: "Complete brain dump page with all 8 fixes"
      contains: "groupByDay"
    - path: "src/hooks/useBrainDump.ts"
      provides: "Brain dump data fetching"
  key_links:
    - from: "src/pages/BrainDump.tsx"
      to: "src/lib/colors.ts"
      via: "import getProjectColor"
      pattern: "import.*getProjectColor"
    - from: "src/pages/BrainDump.tsx"
      to: "src/lib/utils.ts"
      via: "import groupByDay"
      pattern: "import.*groupByDay"
---

<objective>
Fix all 8 Brain Dump page issues (BFIX-01 through BFIX-08) and apply overall page polish (VISL-07).

Purpose: Brain Dump is the most-used action (D-03). It needs project selector pills, better submit UX, organized history with day grouping, expanded entry formatting, status progression, auto-growing textarea, and processing shimmer. All per the feedback spec.

Output: BrainDump.tsx fully polished with all 8 fixes applied and inline styles converted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/feedback/PHASE-04-FIXES-SPEC.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md

@src/lib/colors.ts
@src/lib/utils.ts
@src/components/ui/ProjectBadge.tsx
@src/components/ui/StatusBadge.tsx
@src/components/ui/SkeletonBlock.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement project selector, submit UX, textarea auto-grow, and processing shimmer (BFIX-01, 02, 06, 07)</name>
  <files>src/pages/BrainDump.tsx, src/hooks/useBrainDump.ts</files>
  <read_first>
    src/pages/BrainDump.tsx, src/hooks/useBrainDump.ts, src/hooks/useProjects.ts,
    src/data/mock.ts, src/components/ui/SkeletonBlock.tsx
  </read_first>
  <action>
    **BFIX-01: Project selector pill row**
    - Add a row of selectable project pills above the textarea
    - Pills: "Auto-Route" (default, neutral), "Ridgeline", "CLARITY", "Forge Console", "Meridian", "Atlas", "General"
    - Each pill uses project color from `getProjectColor(slug)` for its background/text when selected
    - Unselected pills: `bg-[hsl(var(--bg-elevated))] text-[hsl(var(--text-secondary))]`
    - Selected pill: `bg-[hsl(var(--project-{slug}-bg))] text-[hsl(var(--project-{slug}))]` with a subtle border
    - State: `const [selectedProject, setSelectedProject] = useState<string>('auto')` -- persists between submissions (do NOT reset on submit)
    - Pass selected project to the brain dump mutation/submission handler
    - Use `useProjects` hook to get project list, or hardcode the 5 projects + Auto-Route + General
    - Layout: flex wrap with gap-2, below "Send to:" label (12px, text-tertiary, uppercase, font-medium)

    **BFIX-02: Submit button + keyboard shortcut**
    - Make submit button full-width on mobile (`w-full md:w-auto md:min-w-[120px]`)
    - Add keyboard shortcut hint inside textarea as a positioned ghost text element: absolute bottom-right, "Cmd+Enter" in 11px `text-[hsl(var(--text-tertiary))]`
    - Add actual Cmd+Enter keyboard handler on the textarea: `onKeyDown` checks for `(e.metaKey || e.ctrlKey) && e.key === 'Enter'` and triggers submit
    - When textarea has content (length > 0), add subtle coral glow to submit button: `shadow-[0_0_12px_hsl(var(--accent-coral)/0.3)]`

    **BFIX-06: Textarea auto-grow**
    - Set initial height to 120px via `min-h-[120px]`
    - Use `useRef` and `onInput` handler to auto-grow:
      ```
      const textareaRef = useRef<HTMLTextAreaElement>(null)
      function handleInput() {
        const el = textareaRef.current
        if (el) {
          el.style.height = 'auto'
          el.style.height = Math.min(el.scrollHeight, window.innerHeight * 0.5) + 'px'
        }
      }
      ```
    - Set `overflow-y: auto` so it scrolls beyond 50vh
    - Change placeholder to "What's on your mind?" (shorter per spec)
    - Move instructional text ("Capture thoughts, tasks, ideas...") to PageShell subtitle

    **BFIX-07: Processing shimmer**
    - During brain dump mutation (while isPending/isLoading from the mutation):
      - Show a skeleton shimmer block in the parsed output area using `<SkeletonBlock />` from `@/components/ui/SkeletonBlock`
      - Add "Parsing with Claude..." label below the shimmer in 12px `text-[hsl(var(--text-tertiary))]` with a subtle fade animation
    - When mutation completes, shimmer resolves to actual content with Framer Motion `animate={{ opacity: [0, 1] }}` fade-in

    **Inline style cleanup (D-12):** Convert ALL `style={{}}` objects in BrainDump.tsx to Tailwind utilities.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/BrainDump.tsx contains "selectedProject" state variable
    - src/pages/BrainDump.tsx contains "Auto-Route" or "auto-route" text for default selector
    - src/pages/BrainDump.tsx contains "metaKey" or "ctrlKey" (keyboard shortcut handler)
    - src/pages/BrainDump.tsx contains "min-h-[120px]" (textarea starting height)
    - src/pages/BrainDump.tsx contains "What's on your mind" (shortened placeholder)
    - src/pages/BrainDump.tsx contains "SkeletonBlock" or "Parsing" (processing shimmer)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Project selector pills visible, submit has keyboard shortcut, textarea auto-grows from 120px, processing shows shimmer with label.</done>
</task>

<task type="auto">
  <name>Task 2: Implement history grouping, entry formatting, status progression, and project borders (BFIX-03, 04, 05, 08)</name>
  <files>src/pages/BrainDump.tsx</files>
  <read_first>
    src/pages/BrainDump.tsx, src/lib/utils.ts, src/lib/colors.ts,
    src/components/ui/ProjectBadge.tsx, src/components/ui/badge.tsx
  </read_first>
  <action>
    **BFIX-08: History grouped by day**
    - Import `groupByDay` from `@/lib/utils`
    - In the history section, group brain dump entries by day:
      ```
      const grouped = groupByDay(brainDumps)
      ```
    - Render each group with a sticky header:
      ```
      <div className="sticky top-0 z-10 bg-[hsl(var(--bg-root))] py-2 text-[15px] font-semibold text-[hsl(var(--text-primary))]">
        {group.label}
      </div>
      ```
    - Within each group, show actual time next to each entry using `formatTime` from utils

    **BFIX-03: Project-colored left borders on history entries**
    - Each brain dump entry card gets a 4px left border in the project color:
      `border-l-4` with `style={{ borderLeftColor: getProjectColorVar(entry.project_slug ?? 'general') }}`
    - Status badges with more visual weight:
      - "Pending": use Badge variant="warning"
      - "Processed"/"Parsed": use Badge variant="success"
      - "Failed": use Badge variant="error"

    **BFIX-04: Expanded entry parsed output formatting**
    - When an entry is expanded (clicked), show:
      1. Original raw text in a subtle blockquote style: `border-l-2 border-[hsl(var(--border-subtle))] pl-4 text-[hsl(var(--text-secondary))] italic`
      2. A visual divider: `<Separator className="my-4" />`
      3. Parsed output section with header "Parsed Output" in 13px font-semibold
      4. Each parsed task as a mini card: `p-3 rounded-lg bg-[hsl(var(--bg-elevated))] border border-[hsl(var(--border-subtle))]` containing:
         - Task title (14px font-medium)
         - Project badge using `<ProjectBadge project={task.project} />`
         - Priority indicator using `<PriorityBadge priority={task.priority} />`
         - "Add to project" ghost button (non-functional placeholder -- actual functionality is Phase 5)
    - Use Framer Motion for expand/collapse: `AnimatePresence` with `motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }}`

    **BFIX-05: Status progression**
    - Add a status progression indicator on each entry showing the pipeline stage:
      - Stages: Captured, Parsed, Tasks Created, Actioned
      - Render as small pills in a row, each 11px uppercase:
        - Completed stages: filled with status-success bg + green text
        - Current stage: filled with status-info bg + blue text
        - Future stages: outline only, text-tertiary
      - Determine current stage from entry data:
        - Has `created_at` -> Captured (always true)
        - Has `parsed_output` or `status === 'processed'` -> Parsed
        - Has tasks linked -> Tasks Created
        - Tasks are assigned/completed -> Actioned
    - Place below the entry header, above expanded content

    **Inline style cleanup (D-12):** Ensure NO `style={{}}` remains except the justified `borderLeftColor` for dynamic project colors.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/BrainDump.tsx contains "groupByDay" import
    - src/pages/BrainDump.tsx contains "sticky" (day group sticky headers)
    - src/pages/BrainDump.tsx contains "border-l-4" (project-colored left borders)
    - src/pages/BrainDump.tsx contains "Captured" and "Parsed" (status progression stages)
    - src/pages/BrainDump.tsx contains "AnimatePresence" (expand/collapse animation)
    - src/pages/BrainDump.tsx contains "Add to project" (parsed task action button placeholder)
    - npm run build exits 0
  </acceptance_criteria>
  <done>History grouped by day with sticky headers, entries have project-colored borders, expanded entries show formatted parsed output with task cards, status progression pills visible on each entry.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Brain Dump page renders project selector pills above textarea
- History is grouped by day with "Today", "Yesterday", date labels
- Entries have project-colored left borders
- Expanded entries show parsed output with task cards
- Status progression visible
- Textarea auto-grows from 120px
- Processing state shows shimmer
</verification>

<success_criteria>
- All 8 BFIX items resolved
- VISL-07 (BrainDump polish) achieved
- All inline styles converted to Tailwind (D-12)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-03-SUMMARY.md`
</output>
