---
phase: 04-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/dashboard/StatTilesRow.tsx
  - src/components/dashboard/ProjectQuickGlanceCard.tsx
  - src/components/dashboard/ActionItemsCard.tsx
  - src/components/dashboard/ContentCalendarStrip.tsx
  - src/components/dashboard/SystemHealthStrip.tsx
  - src/pages/Dashboard.tsx
  - src/hooks/useDashboardStats.ts
  - src/pages/Projects.tsx
  - src/pages/ProjectDetail.tsx
autonomous: true
requirements:
  - VISL-05
  - VISL-06
  - DFIX-01
  - DFIX-02
  - DFIX-03
  - DFIX-04
  - DFIX-05
  - DFIX-06
  - DFIX-07
  - DFIX-08

must_haves:
  truths:
    - "Priority badges on project cards show red for high, amber for medium, green for low"
    - "Progress bars are green (on track), amber (behind), or red (critical) based on overdue items"
    - "Content calendar strip shows the next week with content if current week is empty"
    - "View all on action items expands inline instead of navigating away"
    - "Stat tiles have colored left borders matching project recency"
    - "CLARITY countdown shows amber treatment at 14-30d and red with pulse under 14d"
    - "Greeting subtitle is dynamic and contextual from real data"
    - "Projects page has proper card spacing and breathing room"
    - "ProjectDetail page has command-center depth and proper information density"
  artifacts:
    - path: "src/components/dashboard/ProjectQuickGlanceCard.tsx"
      provides: "Project cards with priority badges and health-coded progress bars"
      contains: "PriorityBadge"
    - path: "src/components/dashboard/StatTilesRow.tsx"
      provides: "Stat tiles with recency-colored left borders"
      contains: "getRecencyColor"
    - path: "src/components/dashboard/ContentCalendarStrip.tsx"
      provides: "Intelligent week selection for calendar strip"
      contains: "intelligent"
    - path: "src/components/dashboard/ActionItemsCard.tsx"
      provides: "Inline expand for view all action items"
      contains: "setShowAll"
  key_links:
    - from: "src/components/dashboard/ProjectQuickGlanceCard.tsx"
      to: "src/components/ui/PriorityBadge.tsx"
      via: "import PriorityBadge"
      pattern: "import.*PriorityBadge"
    - from: "src/components/dashboard/StatTilesRow.tsx"
      to: "src/lib/colors.ts"
      via: "import getRecencyColor"
      pattern: "import.*getRecencyColor"
---

<objective>
Fix all 8 dashboard issues (DFIX-01 through DFIX-08) and polish the Projects and ProjectDetail pages (VISL-05, VISL-06).

Purpose: The dashboard is the front door (D-02, D-03). Priority badges, progress bars, calendar intelligence, action item expand, stat tile borders, CLARITY countdown, and dynamic greeting all need fixing. Projects page (28 lines) needs spacing verification. ProjectDetail (477 lines) needs command-center polish and inline style cleanup per D-12.

Output: All dashboard components updated, Projects and ProjectDetail pages polished, all inline styles converted to Tailwind.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/feedback/PHASE-04-FIXES-SPEC.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md

@src/lib/colors.ts
@src/components/ui/PriorityBadge.tsx
@src/components/ui/ProjectBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 8 dashboard component issues (DFIX-01 through DFIX-08)</name>
  <files>
    src/components/dashboard/ProjectQuickGlanceCard.tsx,
    src/components/dashboard/StatTilesRow.tsx,
    src/components/dashboard/ActionItemsCard.tsx,
    src/components/dashboard/ContentCalendarStrip.tsx,
    src/pages/Dashboard.tsx,
    src/hooks/useDashboardStats.ts,
    src/lib/utils.ts
  </files>
  <read_first>
    src/components/dashboard/ProjectQuickGlanceCard.tsx,
    src/components/dashboard/StatTilesRow.tsx,
    src/components/dashboard/ActionItemsCard.tsx,
    src/components/dashboard/ContentCalendarStrip.tsx,
    src/pages/Dashboard.tsx,
    src/hooks/useDashboardStats.ts,
    src/lib/utils.ts,
    src/data/mock.ts
  </read_first>
  <action>
    **DFIX-01: Priority badges (ProjectQuickGlanceCard.tsx)**
    - Replace any existing priority badge rendering with `<PriorityBadge priority={project.priority} />` imported from `@/components/ui/PriorityBadge`
    - In mock data (`src/data/mock.ts`), update Meridian and Atlas projects to have `priority: 'medium'` instead of `'high'`
    - Result: Ridgeline, CLARITY, Forge = red "High"; Meridian, Atlas = amber "Medium"

    **DFIX-02: Progress bar health colors (ProjectQuickGlanceCard.tsx)**
    - Find the progress bar (likely uses shadcn Progress or a div with width%). Change its color based on project health:
      - Count overdue action items for the project (items where due_date < now and status != 'done')
      - If any overdue high-urgency items: `bg-[hsl(var(--status-error))]` (red)
      - If any overdue items: `bg-[hsl(var(--status-warning))]` (amber)
      - If no overdue items: `bg-[hsl(var(--status-success))]` (green)
    - If action item data is not available in the component, use a simpler heuristic based on progress percentage: <30% = red, 30-70% = amber, >70% = green

    **DFIX-03: Intelligent week selection (ContentCalendarStrip.tsx)**
    - Find the logic that determines which week to display
    - Change to: check if current week has any content items. If yes, show current week. If no, find next week that has content. If no future content, show current week with CTA message "No content scheduled. Plan your next week."
    - Update the header label to show "This Week" or "Next scheduled: Week N (Mon DD - Sun DD)"
    - Show already-posted content from earlier in the week with a green checkmark icon overlay

    **DFIX-04: View all expands inline (ActionItemsCard.tsx)**
    - Replace the `<Link to="/activity">` (or similar navigation) on "View all" with an `onClick` handler
    - Add state: `const [showAll, setShowAll] = useState(false)`
    - When collapsed, show first 5 items. When expanded, show all items
    - "View all (N)" becomes expand trigger. When expanded, show "Collapse" button at bottom
    - Animate expansion with Framer Motion `AnimatePresence` and `motion.div` with `layout` prop

    **DFIX-05: Stat tile recency borders (StatTilesRow.tsx)**
    - Import `getRecencyColor` from `@/lib/colors`
    - For each project stat tile, determine last activity date from the data available (useDashboardStats hook or passed props)
    - Add a 4px left border with recency color: `border-l-4 border-l-[hsl(var(--status-success))]` for green, `border-l-[hsl(var(--status-warning))]` for amber, `border-l-[hsl(var(--status-error))]` for red
    - Use `getRecencyColor(lastActivityDate)` to determine which status color to use

    **DFIX-06: CLARITY countdown urgency (StatTilesRow.tsx or ProjectQuickGlanceCard.tsx)**
    - Find where CLARITY countdown is rendered (likely in stat tiles or project card)
    - Calculate days until CLARITY launch (the launch date should be in project data or mock data)
    - Thresholds:
      - 30+ days: normal styling, no special treatment
      - 14-30 days: amber number color `text-[hsl(var(--status-warning))]`, amber border, show date below
      - Under 14 days: red number `text-[hsl(var(--status-error))]`, add Framer Motion `animate={{ scale: [1, 1.02, 1] }}` with `transition={{ repeat: Infinity, duration: 5 }}` for subtle pulse
      - Launch day: green "Today!" text with `text-[hsl(var(--status-success))]`
      - Post-launch: green checkmark + "Launched" text

    **DFIX-07: Recency color verification (ProjectQuickGlanceCard.tsx, useDashboardStats.ts)**
    - Check how recency/last activity is calculated. It MUST use `activity_log` most recent entry per project, NOT `project.updated_at`
    - If the hook uses `project.updated_at`, change the query to join or separately query `activity_log` for `MAX(created_at) WHERE project_id = X`
    - Verify thresholds: green < 48h, amber 48h-5d (120h), red 5d+
    - Import and use `getRecencyColor` from `@/lib/colors` for consistent threshold logic

    **DFIX-08: Dynamic greeting subtitle (Dashboard.tsx or a dashboard component)**
    - Add a `getDynamicSubtitle` function to `src/lib/utils.ts`:
      ```typescript
      export function getDynamicSubtitle(stats: {
        actionItemCount: number
        upcomingDeadlineDays?: number
        upcomingDeadlineProject?: string
      }): string {
        const hour = new Date().getHours()
        const isWeekend = [0, 6].includes(new Date().getDay())
        if (stats.upcomingDeadlineDays !== undefined && stats.upcomingDeadlineDays <= 7) {
          return `${stats.upcomingDeadlineProject} launches in ${stats.upcomingDeadlineDays} days`
        }
        if (stats.actionItemCount > 0) {
          return `${stats.actionItemCount} action items need your attention`
        }
        if (isWeekend) return 'Weekend mode -- review and plan ahead'
        if (hour < 9) return 'Early start today'
        return 'All clear -- time for deep work'
      }
      ```
    - Use this in the Dashboard greeting area. Subtitle should be 14px with `text-[hsl(var(--text-secondary))]` (not tertiary)

    **Inline style cleanup (D-12):** Convert ALL `style={{}}` objects in every file touched to Tailwind utility classes. Remove any `<style>` tag injections. Convert inline HSL color strings to CSS variable references.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/ProjectQuickGlanceCard.tsx contains "PriorityBadge"
    - src/components/dashboard/ProjectQuickGlanceCard.tsx contains "status-success" or "status-warning" or "status-error" (progress bar colors)
    - src/components/dashboard/ContentCalendarStrip.tsx contains logic for checking content in current week
    - src/components/dashboard/ActionItemsCard.tsx contains "showAll" state variable
    - src/components/dashboard/ActionItemsCard.tsx does NOT contain "Link to" or "href" for view-all (no navigation)
    - src/components/dashboard/StatTilesRow.tsx contains "getRecencyColor" or "border-l-4"
    - src/lib/utils.ts contains "export function getDynamicSubtitle"
    - src/data/mock.ts contains Meridian or Atlas with priority "medium"
    - npm run build exits 0
  </acceptance_criteria>
  <done>All 8 DFIX items implemented: priority badges colored, progress bars health-coded, calendar intelligent, action items expand inline, stat tiles have recency borders, CLARITY countdown has urgency treatment, recency verified against activity_log, greeting is dynamic.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Projects and ProjectDetail pages (VISL-05, VISL-06) with inline style cleanup</name>
  <files>
    src/pages/Projects.tsx, src/pages/ProjectDetail.tsx
  </files>
  <read_first>
    src/pages/Projects.tsx, src/pages/ProjectDetail.tsx,
    src/components/layout/PageShell.tsx
  </read_first>
  <action>
    **VISL-05: Projects page polish (Projects.tsx - 28 lines)**
    - Verify card spacing uses Phase 2 standards: 24px padding, 14px radius, 1px warm border, hover shadow (D-18)
    - Verify typography ladder: 28px page title, 15px card titles, 14px body (D-19)
    - Verify section gaps are 32-40px between sections, 20-24px between cards (gap-5 or gap-6)
    - Convert any inline `style={{}}` to Tailwind classes (D-12)
    - If project cards exist on this page, import and use `ProjectBadge` for project identity
    - This is a small file (28 lines) so changes may be minimal -- verify and adjust

    **VISL-06: ProjectDetail page polish (ProjectDetail.tsx - 477 lines)**
    - This is the largest page file. Systematically convert ALL inline `style={{}}` objects to Tailwind utilities (D-12, D-14)
    - Verify Phase 2 standards throughout:
      - Card padding: p-6 (24px)
      - Card radius: rounded-lg (14px)
      - Card border: border border-[hsl(var(--border-subtle))]
      - Typography: h1 28px, section headers 18px (text-lg font-semibold), card titles 15px, body 14px, meta 12px
      - Section gaps: space-y-8 or space-y-10 (32-40px)
      - Card gaps: gap-5 or gap-6 (20-24px)
    - Replace any inline color styles like `style={{ color: 'hsl(var(--accent-coral))' }}` with `text-[hsl(var(--accent-coral))]` (D-14)
    - Replace any `<style>` tag injections with Tailwind responsive utilities (D-13)
    - Ensure project identity uses `getProjectColor` where project slug/name is displayed
    - Import and use PriorityBadge for any priority displays
    - Import and use ProjectBadge for project name badges
    - Check for command-center depth: information density should be high but organized. Sections should have clear headers, data should be scannable, related items grouped visually

    **General for both files:**
    - No remaining `style={{` in either file after this task (except justified cases documented with a comment)
    - All colors use CSS variable references, not hardcoded values
    - Framer Motion used for any section animations (motion.div with fadeIn)
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ProjectDetail.tsx has zero or near-zero `style={{` occurrences (grep count <= 2, any remaining must have justification comment)
    - src/pages/Projects.tsx has zero `style={{` occurrences
    - src/pages/ProjectDetail.tsx contains "p-6" or "padding" class for card padding
    - src/pages/ProjectDetail.tsx contains "rounded-lg" for card radius
    - src/pages/ProjectDetail.tsx contains "border-[hsl(var(--border-subtle))]" or equivalent
    - npm run build exits 0
  </acceptance_criteria>
  <done>Projects page verified against Phase 2 standards. ProjectDetail page fully polished with inline styles converted, typography verified, spacing standardized, and command-center density achieved.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Dashboard shows colored priority badges (not all red)
- Dashboard progress bars show health colors
- Content calendar shows intelligent week selection
- Action items expand inline
- Stat tiles have recency borders
- No inline styles remain in touched files
</verification>

<success_criteria>
- All 8 DFIX items resolved
- VISL-05 (Projects) and VISL-06 (ProjectDetail) polished
- All inline styles in touched files converted to Tailwind (D-12)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-02-SUMMARY.md`
</output>
