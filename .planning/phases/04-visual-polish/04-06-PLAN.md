---
phase: 04-visual-polish
plan: 06
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/ActivityLog.tsx
  - src/hooks/useActivityLog.ts
autonomous: true
requirements:
  - VISL-10
  - AFIX-01
  - AFIX-02
  - AFIX-03
  - AFIX-04
  - AFIX-05
  - AFIX-06
  - AFIX-07
  - AFIX-08
  - AFIX-09
  - AFIX-10
  - AFIX-11
  - AFIX-12

must_haves:
  truths:
    - "Activity entries are grouped by day with sticky headers showing date and count"
    - "Timeline dots are color-coded by tool type"
    - "Entries have three-tier visual weight (major, standard, background)"
    - "Filter chips show clear active/inactive state with count badges"
    - "Search is debounced with result count display"
    - "Pagination loads 20 entries per batch with load-more button"
    - "Project badges use consistent project colors"
    - "Activity density bar chart shows 14-day history at top"
  artifacts:
    - path: "src/pages/ActivityLog.tsx"
      provides: "Complete activity log with all 12 fixes"
      contains: "groupByDay"
    - path: "src/hooks/useActivityLog.ts"
      provides: "Activity log data with pagination support"
  key_links:
    - from: "src/pages/ActivityLog.tsx"
      to: "src/lib/colors.ts"
      via: "import getToolColor, getProjectColor"
      pattern: "import.*getToolColor"
    - from: "src/pages/ActivityLog.tsx"
      to: "src/lib/utils.ts"
      via: "import groupByDay"
      pattern: "import.*groupByDay"
    - from: "src/pages/ActivityLog.tsx"
      to: "src/hooks/useDebounce.ts"
      via: "import useDebounce"
      pattern: "import.*useDebounce"
---

<objective>
Fix all 12 Activity Log issues (AFIX-01 through AFIX-12) and apply overall page polish (VISL-10).

Purpose: Activity Log needs significant structural work: day grouping, tiered visual weight, color-coded timeline, debounced search, pagination, density heatmap, and manual entry form. This transforms it from a flat list into a scannable, organized timeline.

Output: ActivityLog.tsx fully rebuilt with all 12 fixes, useActivityLog.ts extended with pagination.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/feedback/PHASE-04-FIXES-SPEC.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md

@src/lib/colors.ts
@src/lib/utils.ts
@src/hooks/useDebounce.ts
@src/components/ui/ProjectBadge.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Day grouping, tiered visual weight, color-coded timeline, filter chips, and search (AFIX-01 through AFIX-06)</name>
  <files>src/pages/ActivityLog.tsx</files>
  <read_first>
    src/pages/ActivityLog.tsx, src/hooks/useActivityLog.ts, src/data/mock.ts,
    src/lib/utils.ts, src/lib/colors.ts, src/hooks/useDebounce.ts
  </read_first>
  <action>
    **AFIX-01: Day grouping with sticky headers**
    - Import `groupByDay` from `@/lib/utils`
    - Group all activity entries by day: `const grouped = groupByDay(filteredEntries)`
    - Render each group with a sticky header:
      ```jsx
      <div className="sticky top-0 z-10 flex items-center justify-between bg-[hsl(var(--bg-root))] py-3 border-b border-[hsl(var(--border-subtle))]">
        <span className="text-[15px] font-semibold text-[hsl(var(--text-primary))]">{group.label}</span>
        <span className="text-xs text-[hsl(var(--text-tertiary))]">{group.items.length} entries</span>
      </div>
      ```
    - 32px spacing between groups: `space-y-8` on the group container
    - Horizontal rule between groups: the border-b on the sticky header serves this purpose

    **AFIX-02: Color-coded timeline dots**
    - Import `getToolColor` from `@/lib/colors`
    - Each entry has a timeline dot on the left. Color based on tool type:
      - Claude Code: coral (`accent-coral`)
      - n8n: green (`status-success`)
      - Slack: amber (`status-warning`)
      - System: gray (`text-tertiary`)
      - Manual: navy (`accent-navy`)
      - Cowork: blue (`status-info`)
    - Render dot: `<div className="w-2 h-2 rounded-full" style={{ backgroundColor: \`hsl(var(--\${getToolColor(entry.tool)}))\` }} />`
    - Significant events (entry.type includes 'phase', 'deploy', or 'milestone'): larger dot `w-2.5 h-2.5` with ring: `ring-2 ring-[hsl(var(--${color})/0.2)]`
    - Connecting line between dots: `<div className="absolute left-[3px] top-4 bottom-0 w-px bg-[hsl(var(--border-subtle))]" />`

    **AFIX-03: Three-tier visual weight**
    - Determine tier from entry data:
      - Major (phase completions, deployments, milestones): match by `entry.tool === 'system'` and summary containing 'phase', 'deploy', 'milestone', 'complete'
      - Standard (code sessions, workflow runs): most entries, default tier
      - Background (health checks, pings, minor system events): match by summary containing 'health', 'ping', 'check', 'heartbeat'
    - Rendering per tier:
      - Major: full card with border + shadow: `p-4 rounded-lg border border-[hsl(var(--border-subtle))] shadow-sm bg-[hsl(var(--bg-surface))]`, title in 15px font-semibold
      - Standard: compact card: `p-3 rounded-lg bg-[hsl(var(--bg-surface))]`, text in 14px
      - Background: inline text only, no card: `py-1.5 opacity-60`, text in 13px text-tertiary

    **AFIX-04: Filter chips active/inactive state**
    - Filter chips for project and tool filtering
    - Active chip: coral fill + white text for project filters: `bg-[hsl(var(--accent-coral))] text-white`
    - Active tool chip: filled with count badge: `bg-[hsl(var(--bg-active))] text-[hsl(var(--text-primary))]` + `<span className="ml-1 text-xs opacity-70">(24)</span>`
    - Inactive chip: outline only: `border border-[hsl(var(--border-default))] text-[hsl(var(--text-tertiary))] bg-transparent`
    - Chips in a flex row with gap-2, wrapping: `flex flex-wrap gap-2`

    **AFIX-05: Debounced search with feedback (D-16)**
    - Import `useDebounce` from `@/hooks/useDebounce`
    - State: `const [searchQuery, setSearchQuery] = useState('')`
    - `const debouncedQuery = useDebounce(searchQuery, 300)`
    - Filter entries: `const filtered = entries.filter(e => e.summary.toLowerCase().includes(debouncedQuery.toLowerCase()))`
    - Show result count when query is non-empty: `<span className="text-xs text-[hsl(var(--text-tertiary))]">{filtered.length} results for "{debouncedQuery}"</span>`
    - Show subtle loading indicator while `searchQuery !== debouncedQuery` (typing): a small spinner or pulse dot
    - Empty state when no results: "No activities matching '{debouncedQuery}'" with suggestion to clear
    - Search input: shadcn Input with Search icon prefix, `placeholder="Search activities..."`

    **AFIX-06: Standardized timestamps**
    - Within 24 hours: relative time ("3h ago") using `formatRelativeTime` from utils
    - Older than 24h but within the same day group: show time only ("2:34 PM") using `formatTime` from utils
    - Show exact datetime in a tooltip on hover (use shadcn Tooltip):
      ```jsx
      <Tooltip>
        <TooltipTrigger>{displayTime}</TooltipTrigger>
        <TooltipContent>{format(new Date(entry.created_at), 'EEEE, MMMM d, yyyy h:mm a')}</TooltipContent>
      </Tooltip>
      ```

    **Inline style cleanup (D-12):** Convert ALL `style={{}}` in ActivityLog.tsx to Tailwind. Exception: timeline dot color uses inline style for dynamic tool colors (justified).
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ActivityLog.tsx contains "groupByDay" import
    - src/pages/ActivityLog.tsx contains "sticky top-0" (day group sticky headers)
    - src/pages/ActivityLog.tsx contains "getToolColor" import
    - src/pages/ActivityLog.tsx contains "opacity-60" or "opacity-70" (background tier)
    - src/pages/ActivityLog.tsx contains "useDebounce" import
    - src/pages/ActivityLog.tsx contains "debouncedQuery" or "debouncedSearch"
    - src/pages/ActivityLog.tsx contains "results for" (search result count display)
    - src/pages/ActivityLog.tsx contains "Tooltip" import (timestamp tooltips)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Activities grouped by day with sticky headers, timeline dots color-coded, three-tier visual weight, filter chips with active states, search debounced with result count, timestamps standardized with tooltips.</done>
</task>

<task type="auto">
  <name>Task 2: Pagination, project badges, density heatmap, tool badges, entry truncation, and manual entry (AFIX-07 through AFIX-12)</name>
  <files>src/pages/ActivityLog.tsx, src/hooks/useActivityLog.ts</files>
  <read_first>
    src/pages/ActivityLog.tsx, src/hooks/useActivityLog.ts, src/data/mock.ts,
    src/components/ui/ProjectBadge.tsx, src/lib/colors.ts
  </read_first>
  <action>
    **AFIX-07: Pagination / load-more**
    - In `useActivityLog.ts`, add pagination support:
      - Add `limit` and `offset` parameters to the query function
      - Add state: page size = 20
      - If using mock data: slice the array `mockData.slice(0, page * 20)`
      - If using Supabase: add `.range(0, page * 20 - 1)` to the query
    - In ActivityLog.tsx:
      - State: `const [page, setPage] = useState(1)`
      - Show "Load more" button at bottom when more entries exist: `<Button variant="outline" onClick={() => setPage(p => p + 1)}>Load more</Button>`
      - Total count in header area: `"{totalCount} activities across all projects"` in 13px text-secondary
    - Alternative simpler approach if hook restructure is complex: just slice the entries array in the component `entries.slice(0, page * 20)` and show Load more

    **AFIX-08: Project badges with consistent colors**
    - Import `ProjectBadge` from `@/components/ui/ProjectBadge`
    - Replace any existing project badge/pill rendering with `<ProjectBadge project={entry.project_slug}>{entry.project_name}</ProjectBadge>`
    - This ensures consistent project colors app-wide

    **AFIX-09: Activity density heatmap/bar chart**
    - Add a 14-day activity bar chart at the top of the page (below filters, above entries)
    - Calculate: for each of the last 14 days, count entries grouped by project
    - Render as a row of vertical stacked bars:
      ```jsx
      <div className="flex items-end gap-1 h-[60px] mb-6">
        {last14Days.map(day => (
          <div key={day.date} className="flex-1 flex flex-col-reverse">
            {day.projectCounts.map(({ slug, count, total }) => (
              <div
                key={slug}
                className="w-full rounded-t-sm"
                style={{
                  height: `${(count / maxDayCount) * 100}%`,
                  backgroundColor: getProjectColorVar(slug)
                }}
              />
            ))}
          </div>
        ))}
      </div>
      ```
    - Hover tooltip on each bar: "March 20: 8 entries (5 forge, 2 clarity, 1 system)"
    - Day labels below in 10px text-tertiary (show abbreviated day name or date)

    **AFIX-10: Tool badges distinct from project badges**
    - Tool badges: different shape from project badges
    - Use rounded-md (rectangle) instead of rounded-full (pill): `<Badge className="rounded-md">`
    - Include a small icon prefix in the badge:
      - Claude Code: Code2 icon (Lucide)
      - n8n: Zap icon (Lucide)
      - Slack: MessageSquare icon (Lucide)
      - System: Settings icon (Lucide)
      - Manual: PenLine icon (Lucide)
    - Style: `bg-[hsl(var(--bg-elevated))] text-[hsl(var(--text-secondary))] text-xs px-2 py-0.5 rounded-md`

    **AFIX-11: Entry text truncation**
    - Entry summary text: single line with `truncate` class
    - Bold first phrase pattern: split summary at first period or dash. Wrap first segment in `<span className="font-semibold">`
    - Click to expand: toggle `truncate` class off, show full text
    - Example: "**Phase 2 complete.** Card/Button migration across 14 files..." -> only bold part visible when truncated, full text on expand
    - State per entry: `expandedEntries` Set tracking which entry IDs are expanded

    **AFIX-12: Manual activity entry "Log activity" button**
    - Add "Log activity" button near the search bar area: `<Button variant="outline" size="sm"><PenLine className="w-4 h-4 mr-1.5" />Log activity</Button>`
    - On click, show an inline form (not modal) that slides down:
      - Project selector dropdown (Select component with all projects)
      - Summary text input
      - Tool type dropdown (default: "Manual")
      - Timestamp (default: now, with date/time input for adjustment)
      - "Save" and "Cancel" buttons
    - On save: call activity log mutation (or show toast "Activity logged" with mock behavior)
    - Animate form appearance with Framer Motion slide-down

    **General polish:**
    - Ensure total page layout follows Phase 2 standards (D-18, D-19)
    - All cards use correct padding, radius, borders
    - Typography ladder verified
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ActivityLog.tsx contains "Load more" button text
    - src/pages/ActivityLog.tsx contains "ProjectBadge" import
    - src/pages/ActivityLog.tsx contains "last14Days" or "14" in density chart logic
    - src/pages/ActivityLog.tsx contains "rounded-md" (tool badge distinct shape)
    - src/pages/ActivityLog.tsx contains "font-semibold" for bold first phrase pattern
    - src/pages/ActivityLog.tsx contains "truncate" for entry text truncation
    - src/pages/ActivityLog.tsx contains "Log activity" button text
    - src/pages/ActivityLog.tsx contains "PenLine" icon import (for manual entry)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Pagination with load-more, project badges use consistent colors, 14-day density bar chart visible, tool badges have distinct rectangular shape with icons, entry text truncated with bold first phrase, manual entry form functional.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Activity Log groups by day with sticky headers
- Timeline dots colored by tool
- Three visual tiers visible
- Search debounced with result count
- Load more button works
- Density chart shows 14-day bars
- Manual entry form appears
</verification>

<success_criteria>
- All 12 AFIX items resolved
- VISL-10 (ActivityLog polish) achieved
- All inline styles converted to Tailwind (D-12)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-06-SUMMARY.md`
</output>
