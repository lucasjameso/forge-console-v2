---
phase: 02-global-design-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/components/ui/card.tsx
  - src/components/ui/button.tsx
  - tailwind.config.ts
autonomous: true
requirements: [VISL-02, VISL-03]

must_haves:
  truths:
    - "Warm cream/beige palette is visible on all backgrounds (root, elevated, active, sidebar)"
    - "Card component renders with 14px radius and shadow-card by default"
    - "Button default variant renders coral, ghost variant hovers to secondary (not navy)"
    - "Typography classes cover the full 8-step ladder from 11px overline to 36px stat"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Warm palette tokens, new typography classes, warm border tokens"
      contains: "--bg-root: 30 33% 94%"
    - path: "src/components/ui/card.tsx"
      provides: "Fixed Card component with rounded-lg and shadow-card"
      contains: "rounded-lg"
    - path: "src/components/ui/button.tsx"
      provides: "Button with coral default, fixed ghost hover"
      contains: "hover:bg-secondary"
  key_links:
    - from: "src/styles/globals.css"
      to: "src/components/ui/card.tsx"
      via: "CSS variable bridge (--card references --bg-surface)"
      pattern: "--card: var\\(--bg-surface\\)"
    - from: "src/styles/globals.css"
      to: "src/components/ui/button.tsx"
      via: "CSS variable bridge (--primary references --accent-coral)"
      pattern: "--primary: var\\(--accent-coral\\)"
---

<objective>
Update the design token foundation to warm cream/beige palette, fix the shadcn Card and Button components to match spec, and add the two missing typography utility classes.

Purpose: Every subsequent plan in this phase (and Phases 3-4) builds on these corrected primitives. Fixing them first means one change propagates everywhere.
Output: Updated globals.css with warm tokens + new typography classes, corrected card.tsx and button.tsx components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-global-design-standards/02-CONTEXT.md
@.planning/phases/02-global-design-standards/02-RESEARCH.md

<interfaces>
<!-- Key files the executor will modify. Read these before any changes. -->

From src/styles/globals.css (current token block, lines 5-77):
- --bg-root: 220 27% 98% (COOL -- must become 30 33% 94%)
- --bg-surface: 0 0% 100% (stays unchanged)
- --bg-elevated: 220 27% 96% (COOL -- must become 30 26% 89%)
- --bg-active: 218 21% 93% (COOL -- must become 32 22% 86%)
- --border-subtle: 220 6% 92% (COOL -- must become 30 10% 94%)
- --border-default: 220 6% 88% (COOL -- must become 30 10% 91%)
- --border-strong: 216 6% 83% (COOL -- must become 30 10% 87%)
- shadcn bridge section uses var() references (lines 57-77) -- these auto-update when Forge tokens change

From src/components/ui/card.tsx (line 12):
  className: "rounded-xl border bg-card text-card-foreground shadow"
  Must become: "rounded-lg border bg-card text-card-foreground shadow-card"

From src/components/ui/button.tsx (lines 11-21):
  variant default: "bg-primary text-primary-foreground shadow hover:bg-primary/90"
  variant outline: "border border-input bg-background shadow-sm hover:bg-accent hover:text-accent-foreground"
  variant ghost: "hover:bg-accent hover:text-accent-foreground"
  -- outline and ghost both hover to --accent (navy). Must hover to --secondary instead.

From tailwind.config.ts (lines 25-30):
  borderRadius.lg = calc(var(--radius) + 4px) = 14px (correct for spec)
  borderRadius.xl = calc(var(--radius) + 8px) = 18px (too large, card was using this)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Warm palette tokens and typography classes in globals.css</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current state of all tokens and typography classes)
    - .planning/phases/02-global-design-standards/02-CONTEXT.md (D-18 through D-22 warm palette decisions, D-10 through D-13 typography decisions)
  </read_first>
  <action>
Update the `:root` token block in globals.css with these exact replacements (per D-18, D-19, D-20, D-22):

**Background tokens (replace cool grays with warm cream/beige):**
- `--bg-root: 220 27% 98%` --> `--bg-root: 30 33% 94%`
- `--bg-elevated: 220 27% 96%` --> `--bg-elevated: 30 26% 89%`
- `--bg-active: 218 21% 93%` --> `--bg-active: 32 22% 86%`
- Add NEW token: `--bg-sidebar: 33 28% 92%` (after --bg-active)
- `--bg-surface: 0 0% 100%` stays UNCHANGED (per D-19)

**Border tokens (replace cool tint with warm tint):**
- `--border-subtle: 220 6% 92%` --> `--border-subtle: 30 10% 94%`
- `--border-default: 220 6% 88%` --> `--border-default: 30 10% 91%`
- `--border-strong: 216 6% 83%` --> `--border-strong: 30 10% 87%`

**Text tokens:** UNCHANGED (per D-21)
**Accent tokens:** UNCHANGED (per D-21)
**Status/shadow/sidebar-width/radius tokens:** UNCHANGED

**shadcn bridge section:** Verify all bridge variables still use `var()` references to Forge tokens. The warm values auto-propagate through the bridge. No manual HSL values should exist in the bridge section.

**Typography classes (per D-10, D-11):**
Add two new classes in the `@layer components` section, after the existing `.text-stat` class:

```css
.text-body-sm {
  font-size: 13px;
  font-weight: 400;
  color: hsl(var(--text-primary));
}

.text-overline {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: hsl(var(--text-tertiary));
}
```

Also add responsive overrides in the `@media (max-width: 768px)` block:
```css
.text-body-sm {
  font-size: 12px;
}
```

Do NOT touch the responsive `--sidebar-width` overrides, scrollbar styles, selection styles, or status-dot classes.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && grep -c "30 33% 94%" src/styles/globals.css && grep -c "text-body-sm" src/styles/globals.css && grep -c "text-overline" src/styles/globals.css && grep -c "bg-sidebar" src/styles/globals.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--bg-root: 30 33% 94%`
    - globals.css contains `--bg-elevated: 30 26% 89%`
    - globals.css contains `--bg-active: 32 22% 86%`
    - globals.css contains `--bg-sidebar: 33 28% 92%`
    - globals.css contains `--border-subtle: 30 10% 94%`
    - globals.css contains `--border-default: 30 10% 91%`
    - globals.css contains `--border-strong: 30 10% 87%`
    - globals.css contains `.text-body-sm` with `font-size: 13px`
    - globals.css contains `.text-overline` with `font-size: 11px` and `text-transform: uppercase`
    - globals.css bridge section contains NO raw HSL values (only var() references)
    - `--bg-surface: 0 0% 100%` unchanged
    - `--text-primary`, `--text-secondary`, `--text-tertiary` unchanged
    - `--accent-coral`, `--accent-navy` unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All background and border tokens updated to warm palette, two new typography classes added, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Fix Card component and update Button variants</name>
  <files>src/components/ui/card.tsx, src/components/ui/button.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (current rounded-xl and shadow values)
    - src/components/ui/button.tsx (current variant definitions)
    - .planning/phases/02-global-design-standards/02-CONTEXT.md (D-05 card fix, D-16 button migration decisions)
    - .planning/phases/02-global-design-standards/02-RESEARCH.md (Pitfall 5: ghost button hover color)
  </read_first>
  <action>
**Card component fix (per D-05):**
In `card.tsx`, change the Card component's className string on line 12:
- FROM: `"rounded-xl border bg-card text-card-foreground shadow"`
- TO: `"rounded-lg border bg-card text-card-foreground shadow-card"`

Two changes: `rounded-xl` becomes `rounded-lg` (18px to 14px radius), `shadow` becomes `shadow-card` (uses the defined shadow token).

Do NOT change CardHeader, CardTitle, CardDescription, CardContent, or CardFooter.

**Button variant fixes (per D-16 and research Pitfall 5):**
In `button.tsx`, update three variants in the `buttonVariants` CVA definition:

1. `outline` variant:
   - FROM: `"border border-input bg-background shadow-sm hover:bg-accent hover:text-accent-foreground"`
   - TO: `"border border-input bg-background shadow-sm hover:bg-secondary hover:text-secondary-foreground"`
   (Changes hover from navy accent to neutral secondary background)

2. `ghost` variant:
   - FROM: `"hover:bg-accent hover:text-accent-foreground"`
   - TO: `"hover:bg-secondary hover:text-secondary-foreground"`
   (Changes hover from navy accent to neutral secondary background)

3. `default` variant stays UNCHANGED -- `bg-primary` already maps to coral via the bridge.

4. `destructive`, `secondary`, `link` variants stay UNCHANGED.

5. All `size` variants stay UNCHANGED.

Add `cursor-pointer` to the base CVA string (the first argument to `cva()`) so all Button variants get pointer cursor. Insert it after `disabled:opacity-50`.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && grep "rounded-lg" src/components/ui/card.tsx && grep "shadow-card" src/components/ui/card.tsx && grep "hover:bg-secondary" src/components/ui/button.tsx && grep "cursor-pointer" src/components/ui/button.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - card.tsx Card className contains `rounded-lg` (not `rounded-xl`)
    - card.tsx Card className contains `shadow-card` (not bare `shadow`)
    - button.tsx outline variant contains `hover:bg-secondary hover:text-secondary-foreground`
    - button.tsx ghost variant contains `hover:bg-secondary hover:text-secondary-foreground`
    - button.tsx default variant still contains `bg-primary` (unchanged)
    - button.tsx base string contains `cursor-pointer`
    - button.tsx does NOT contain `hover:bg-accent` in any variant
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Card renders at 14px radius with shadow-card, Button ghost/outline hover to secondary instead of navy, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep "rounded-xl" src/components/ui/card.tsx` returns nothing (old radius gone)
- `grep "hover:bg-accent" src/components/ui/button.tsx` returns nothing (old hover gone)
- `grep "220 27% 98%" src/styles/globals.css` returns nothing (old cool gray gone)
- `grep "30 33% 94%" src/styles/globals.css` returns 1 match (warm root token)
- `grep "text-body-sm" src/styles/globals.css` returns match
- `grep "text-overline" src/styles/globals.css` returns match
</verification>

<success_criteria>
1. All background tokens use warm cream/beige HSL values (30-33 hue range)
2. All border tokens use warm tint (30 hue)
3. Card component defaults to rounded-lg (14px) and shadow-card
4. Button ghost and outline variants hover to secondary (neutral) not accent (navy)
5. Typography system has 8 classes covering 11px through 36px
6. Build compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-global-design-standards/02-01-SUMMARY.md`
</output>
