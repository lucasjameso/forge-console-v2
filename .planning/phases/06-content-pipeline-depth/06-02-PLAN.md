---
phase: 06-content-pipeline-depth
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/pipeline/DraggableContentCard.tsx
  - src/components/pipeline/DroppableColumn.tsx
  - src/components/pipeline/DroppableDateCell.tsx
  - src/components/pipeline/DragOverlayCard.tsx
  - src/components/pipeline/BulkActionBar.tsx
  - src/pages/ContentPipeline.tsx
  - src/hooks/useContentReviews.ts
autonomous: true
requirements:
  - CSUG-02
  - CSUG-05

must_haves:
  truths:
    - "Kanban view cards can be dragged between status columns (Draft, Pending Review, Approved, Posted) per D-01"
    - "Existing click-to-move dropdown still works alongside drag per D-02"
    - "Month view cards can be dragged between date cells to reschedule per D-03"
    - "Week view has NO drag behavior per D-04"
    - "List view has NO drag behavior per D-05"
    - "Checkbox appears on content cards and floating action bar appears when items selected per D-23/D-24"
    - "Dropping a card in Kanban updates status in Supabase and shows toast"
    - "Dropping a card in Month view updates scheduled_date in Supabase and shows toast"
  artifacts:
    - path: "src/components/pipeline/DraggableContentCard.tsx"
      provides: "Content card with useDraggable and checkbox"
      contains: "useDraggable"
    - path: "src/components/pipeline/DroppableColumn.tsx"
      provides: "Kanban status column droppable"
      contains: "useDroppable"
    - path: "src/components/pipeline/DroppableDateCell.tsx"
      provides: "Month view date cell droppable"
      contains: "useDroppable"
    - path: "src/components/pipeline/DragOverlayCard.tsx"
      provides: "Ghost card during drag"
      contains: "DragOverlayCard"
    - path: "src/components/pipeline/BulkActionBar.tsx"
      provides: "Floating action bar for bulk operations"
      contains: "BulkActionBar"
  key_links:
    - from: "src/pages/ContentPipeline.tsx"
      to: "@dnd-kit/core"
      via: "DndContext wrapping Kanban and Month views"
      pattern: "DndContext"
    - from: "src/components/pipeline/DraggableContentCard.tsx"
      to: "src/hooks/useBulkSelection.ts"
      via: "checkbox toggle"
      pattern: "useBulkSelection|isSelected"
    - from: "src/pages/ContentPipeline.tsx"
      to: "src/hooks/useContentReviews.ts"
      via: "useUpdateContentStatus for Kanban drop, useUpdateScheduledDate for Month drop"
      pattern: "useUpdateContentStatus|useUpdateScheduledDate"
---

<objective>
Wire drag-and-drop into Kanban view (between status columns) and Month view (between date cells), plus bulk selection with floating action bar. This is the marquee interaction feature of Phase 6.

Purpose: Content items become draggable for fast rescheduling and status changes, replacing the slower dropdown-only workflow.
Output: 5 new components, ContentPipeline.tsx refactored with DndContext wrappers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-pipeline-depth/06-CONTEXT.md
@.planning/phases/06-content-pipeline-depth/06-RESEARCH.md
@.planning/phases/06-content-pipeline-depth/06-01-SUMMARY.md

@src/pages/ContentPipeline.tsx
@src/components/pipeline/ContentCard.tsx
@src/components/pipeline/ContentReviewModal.tsx
@src/hooks/useContentReviews.ts
@src/hooks/useBulkSelection.ts
@src/types/database.ts

<interfaces>
From src/hooks/useBulkSelection.ts (created in Plan 01):
```typescript
export function useBulkSelection(): {
  selectedIds: Set<string>
  toggle: (id: string) => void
  selectAll: (ids: string[]) => void
  deselectAll: () => void
  count: number
  isSelected: (id: string) => boolean
}
```

From src/hooks/useContentReviews.ts (extended in Plan 01):
```typescript
export function useUpdateContentStatus(): UseMutationResult  // existing
export function useUpdateScheduledDate(): UseMutationResult   // new from Plan 01
export function useUpdateCaption(): UseMutationResult         // new from Plan 01
```

From @dnd-kit/core:
```typescript
import { DndContext, DragOverlay, closestCorners, closestCenter, PointerSensor, useSensor, useSensors } from '@dnd-kit/core'
import { useDroppable } from '@dnd-kit/core'
import { useDraggable } from '@dnd-kit/core'
import type { DragStartEvent, DragEndEvent } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drag-drop components (DraggableContentCard, DroppableColumn, DroppableDateCell, DragOverlayCard)</name>
  <read_first>
    - src/components/pipeline/ContentCard.tsx
    - src/pages/ContentPipeline.tsx
    - src/types/database.ts
    - src/hooks/useBulkSelection.ts
  </read_first>
  <files>
    src/components/pipeline/DraggableContentCard.tsx,
    src/components/pipeline/DroppableColumn.tsx,
    src/components/pipeline/DroppableDateCell.tsx,
    src/components/pipeline/DragOverlayCard.tsx
  </files>
  <action>
**1. Create src/components/pipeline/DraggableContentCard.tsx:**
- Props: `{ item: ContentReview, onSelect: (item: ContentReview) => void, isSelected: boolean, onToggleSelect: (id: string) => void, showCheckbox: 'always' | 'hover' | 'none' }`
- Uses `useDraggable({ id: item.id })` from @dnd-kit/core
- Wraps existing content card rendering (title, status badge, platform tags, date, slide info) in a div with `ref={setNodeRef}`, spread `{...listeners}`, `{...attributes}`
- Applies transform via inline style: `transform ? { transform: CSS.Translate.toString(transform) } : undefined` using `@dnd-kit/utilities`
- Per D-06: uses @dnd-kit/core useDraggable (NOT sortable for the card itself -- sortable is for column context)
- Adds a checkbox at top-left: when showCheckbox='always', always visible. When showCheckbox='hover', uses group/group-hover Tailwind pattern. When showCheckbox='none', no checkbox.
- Checkbox is a simple `<input type="checkbox" checked={isSelected} onChange={() => onToggleSelect(item.id)} />` styled with accent-coral color
- onClick on the card body (not checkbox, not drag handle) calls onSelect(item)
- Per research pitfall 4: PointerSensor activationConstraint distance=8 is set at the DndContext level, so clicks still work
- Card visual: same styling as existing ContentCard (Card component, padding, StatusBadge, PlatformTags, date, SlideInfo)
- During drag (when isDragging is true from useDraggable): apply `opacity-50` to indicate the original position

**2. Create src/components/pipeline/DroppableColumn.tsx:**
- Props: `{ id: string, label: string, count: number, children: React.ReactNode }`
- Uses `useDroppable({ id })` from @dnd-kit/core
- Renders a column container with: header (label + count badge), scrollable card area (max-h-[600px] overflow-y-auto)
- When `isOver` is true: highlight with `bg-[hsl(var(--accent-coral)/0.05)] border-[hsl(var(--accent-coral)/0.3)]`
- Normal state: Card component with flex flex-col, gap-3 for cards inside

**3. Create src/components/pipeline/DroppableDateCell.tsx:**
- Props: `{ dateKey: string, isCurrentMonth: boolean, isToday: boolean, dayNumber: number, children: React.ReactNode }`
- Uses `useDroppable({ id: dateKey })` where dateKey is formatted as 'yyyy-MM-dd'
- Renders a cell with: day number in top-left, children (content cards) below
- When `isOver` is true: highlight with `bg-[hsl(var(--accent-coral)/0.08)]` and dashed border
- isCurrentMonth=false cells get `opacity-40`
- isToday cells get `bg-[hsl(var(--accent-coral)/0.04)]` ring highlight (same as existing month view today indicator)

**4. Create src/components/pipeline/DragOverlayCard.tsx:**
- Props: `{ item: ContentReview }`
- Renders a simplified card (title, status badge, platform) with `shadow-xl rotate-2` for visual lift effect
- Uses Framer Motion `motion.div` with spring animation `{ scale: 1.03 }` for pick-up feel
- Max width 280px to prevent overlay from being too large
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pipeline/DraggableContentCard.tsx contains `useDraggable`
    - src/components/pipeline/DraggableContentCard.tsx contains `input type="checkbox"`
    - src/components/pipeline/DraggableContentCard.tsx contains `onToggleSelect`
    - src/components/pipeline/DroppableColumn.tsx contains `useDroppable`
    - src/components/pipeline/DroppableColumn.tsx contains `isOver`
    - src/components/pipeline/DroppableDateCell.tsx contains `useDroppable`
    - src/components/pipeline/DroppableDateCell.tsx contains `dateKey`
    - src/components/pipeline/DragOverlayCard.tsx contains `shadow-xl`
    - src/components/pipeline/DragOverlayCard.tsx contains `motion.div`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>4 drag-drop component files created, all compile, each uses correct dnd-kit hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DndContext into Kanban/Month views, add BulkActionBar, integrate bulk selection</name>
  <read_first>
    - src/pages/ContentPipeline.tsx
    - src/components/pipeline/DraggableContentCard.tsx
    - src/components/pipeline/DroppableColumn.tsx
    - src/components/pipeline/DroppableDateCell.tsx
    - src/components/pipeline/DragOverlayCard.tsx
    - src/hooks/useContentReviews.ts
    - src/hooks/useBulkSelection.ts
  </read_first>
  <files>
    src/components/pipeline/BulkActionBar.tsx,
    src/pages/ContentPipeline.tsx
  </files>
  <action>
**1. Create src/components/pipeline/BulkActionBar.tsx:**
- Props: `{ count: number, onApproveAll: () => void, onMoveToDraft: () => void, onReschedule: () => void, onDelete: () => void, onDeselectAll: () => void }`
- Per D-24: Fixed position bar at bottom of viewport center
- Uses AnimatePresence + motion.div with spring animation (y: 80 -> 0 on enter, 0 -> 80 on exit)
- Layout: flex row with gap-3, items-center. Shows: "{count} selected" text, "Approve All" Button (size=sm), "Move to Draft" Button (variant=outline, size=sm), "Reschedule" Button (variant=outline, size=sm), "Delete" Button (variant=destructive, size=sm), "Deselect" Button (variant=ghost, size=sm)
- Container: `fixed bottom-6 left-1/2 -translate-x-1/2 z-50 flex items-center gap-3 px-5 py-3 rounded-[var(--radius-lg)] bg-[hsl(var(--bg-surface))] border border-[hsl(var(--border-default))] shadow-xl`
- Reschedule button: for now, just shows toast "Reschedule coming soon" (date picker integration in a future iteration). This avoids over-scoping this plan.
- Delete button: shows confirm dialog via window.confirm("Delete {count} items?") before proceeding

**2. Refactor ContentPipeline.tsx Kanban view section:**
- Import DndContext, DragOverlay, closestCorners, PointerSensor, useSensor, useSensors from @dnd-kit/core
- Import SortableContext, verticalListSortingStrategy from @dnd-kit/sortable
- Add state: `const [activeItem, setActiveItem] = useState<ContentReview | null>(null)`
- Configure sensors: `useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }))` per research pitfall 4
- Wrap Kanban grid in `<DndContext sensors={sensors} collisionDetection={closestCorners} onDragStart={handleDragStart} onDragEnd={handleKanbanDragEnd}>`
- handleDragStart: find item by event.active.id, set activeItem
- handleKanbanDragEnd: extract over.id as ContentStatus column. If item's current status !== new status, call updateContentStatus.mutate({ id, status: newStatus }). Clear activeItem.
- Per D-01: spring animation on drop via Framer Motion layoutId on cards
- Per D-02: Keep the existing DropdownMenu (click-to-move) on each card alongside the drag capability
- Each column uses DroppableColumn component. Cards inside use DraggableContentCard with showCheckbox='always' per D-23.
- Add `<DragOverlay>{activeItem && <DragOverlayCard item={activeItem} />}</DragOverlay>` inside DndContext

**3. Refactor ContentPipeline.tsx Month view section:**
- Wrap month grid in separate `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragStart={handleDragStart} onDragEnd={handleMonthDragEnd}>`
- Use closestCenter for month view (uniform cell sizes, per research pitfall 1)
- handleMonthDragEnd: extract over.id as date string (yyyy-MM-dd). Call updateScheduledDate.mutate({ id, scheduled_date: newDate }). Clear activeItem.
- Replace existing date cells with DroppableDateCell component. Content items inside use DraggableContentCard with showCheckbox='hover' per D-23.
- Add DragOverlay inside this DndContext too.
- Per D-03: toast confirms reschedule

**4. DO NOT add DndContext to Week view (D-04) or List view (D-05).** Leave them as-is. In those views, cards use DraggableContentCard with showCheckbox='hover' for bulk selection only (no drag). Actually, for Week/List views, keep using the existing card rendering but add checkbox. Use a simpler wrapper that only has the checkbox, not drag.

**5. Wire bulk selection into ContentPipeline.tsx:**
- Call `useBulkSelection()` at the top of the component
- Pass `isSelected` and `onToggleSelect={selection.toggle}` to all DraggableContentCard instances
- Add `<AnimatePresence><BulkActionBar count={selection.count} ... /></AnimatePresence>` at the bottom of the page
- Wire onApproveAll: loop through selectedIds, call updateContentStatus for each with status='approved', then deselectAll()
- Wire onMoveToDraft: loop through selectedIds, call updateContentStatus for each with status='draft', then deselectAll()
- Wire onDelete: loop through selectedIds, call useDeleteContent for each, then deselectAll(). Add a `useDeleteContent()` exported mutation to src/hooks/useContentReviews.ts: useMutation that takes `{ id: string }`, calls supabase.from('content_reviews').delete().eq('id', id). Toast: "Content deleted". Invalidates ['content-reviews'].
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pipeline/BulkActionBar.tsx contains `Approve All`
    - src/components/pipeline/BulkActionBar.tsx contains `fixed bottom-6`
    - src/components/pipeline/BulkActionBar.tsx contains `AnimatePresence` or `motion.div`
    - src/pages/ContentPipeline.tsx contains `DndContext`
    - src/pages/ContentPipeline.tsx contains `closestCorners`
    - src/pages/ContentPipeline.tsx contains `closestCenter`
    - src/pages/ContentPipeline.tsx contains `DragOverlay`
    - src/pages/ContentPipeline.tsx contains `PointerSensor`
    - src/pages/ContentPipeline.tsx contains `activationConstraint`
    - src/pages/ContentPipeline.tsx contains `useBulkSelection`
    - src/pages/ContentPipeline.tsx contains `BulkActionBar`
    - src/pages/ContentPipeline.tsx contains `DroppableColumn`
    - src/pages/ContentPipeline.tsx contains `DroppableDateCell`
    - src/hooks/useContentReviews.ts contains `export function useDeleteContent`
    - src/hooks/useContentReviews.ts contains `Content deleted`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Kanban drag between columns works. Month view drag between dates works. Click-to-move dropdown preserved. Bulk selection with floating action bar operational. Week and List views have no drag. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Kanban: DndContext wraps columns with closestCorners collision
- Month: DndContext wraps date grid with closestCenter collision
- Week/List: no DndContext wrapper
- BulkActionBar renders when selection.count > 0
- PointerSensor has distance: 8 activation constraint
</verification>

<success_criteria>
- Cards draggable in Kanban between 4 status columns with visual feedback
- Cards draggable in Month view between date cells with reschedule
- Click-to-move dropdown preserved as fallback (per D-02)
- Checkboxes on cards with floating bulk action bar
- No drag in Week or List views (per D-04, D-05)
- DragOverlay ghost card visible during drag
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline-depth/06-02-SUMMARY.md`
</output>
