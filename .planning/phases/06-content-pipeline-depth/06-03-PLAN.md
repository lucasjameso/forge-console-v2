---
phase: 06-content-pipeline-depth
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/pipeline/ContentReviewModal.tsx
  - src/components/pipeline/AiRefinementPanel.tsx
  - src/components/pipeline/RevisionHistory.tsx
  - src/components/pipeline/PerformanceCard.tsx
autonomous: true
requirements:
  - CSUG-03
  - CSUG-04
  - CSUG-06
  - CSUG-08
  - CSUG-09
  - CSUG-11

must_haves:
  truths:
    - "Caption is editable in the detail modal with view/edit toggle per D-28"
    - "Character count shows green 1200-1600, amber outside, red >3000 per D-29"
    - "Caption auto-saves on blur when text has changed per D-30"
    - "Refine with AI button sends to Claude Sonnet and returns 2-3 suggestions per D-07/D-08"
    - "Each suggestion shows type, description, original and revised text per D-08"
    - "Hashtag suggestions are a separate button per D-12"
    - "On approve/reject, n8n webhook fires per D-14/D-15"
    - "Revision history shows version list with dates per D-35"
    - "Compare button shows side-by-side diff with green/red highlighting per D-36"
    - "Revert button restores a previous version per D-36"
    - "Performance card shows for posted items with manual entry per D-32"
    - "Carousel slide placeholders render for carousel items per CSUG-03"
  artifacts:
    - path: "src/components/pipeline/ContentReviewModal.tsx"
      provides: "Extended modal with edit mode, AI, performance, revisions, carousel preview"
      contains: "useUpdateCaption"
    - path: "src/components/pipeline/AiRefinementPanel.tsx"
      provides: "AI suggestion display with accept/reject per suggestion"
      contains: "AiRefinementPanel"
    - path: "src/components/pipeline/RevisionHistory.tsx"
      provides: "Version list and diff viewer"
      contains: "diffWords"
    - path: "src/components/pipeline/PerformanceCard.tsx"
      provides: "Manual metrics entry form for posted items"
      contains: "PerformanceCard"
  key_links:
    - from: "src/components/pipeline/ContentReviewModal.tsx"
      to: "src/hooks/useAiRefinement.ts"
      via: "useAiRefinement mutation for refine/hashtags"
      pattern: "useAiRefinement"
    - from: "src/components/pipeline/ContentReviewModal.tsx"
      to: "src/hooks/useN8nWebhook.ts"
      via: "fireWebhook on approve/reject"
      pattern: "useN8nWebhook|fireWebhook"
    - from: "src/components/pipeline/ContentReviewModal.tsx"
      to: "src/hooks/useContentReviews.ts"
      via: "useUpdateCaption for auto-save"
      pattern: "useUpdateCaption"
    - from: "src/components/pipeline/RevisionHistory.tsx"
      to: "src/hooks/useContentVersions.ts"
      via: "useContentVersions query and useRevertVersion mutation"
      pattern: "useContentVersions|useRevertVersion"
---

<objective>
Transform ContentReviewModal from a read-only viewer into a full content editing hub: inline caption editing with auto-save, AI refinement via Claude Sonnet, hashtag suggestions, n8n webhook notifications, revision history with diff, performance metrics entry, and carousel preview placeholders.

Purpose: The detail modal becomes the primary content editing surface where creators refine, review, and track their content.
Output: Extended ContentReviewModal.tsx, 3 new sub-components (AiRefinementPanel, RevisionHistory, PerformanceCard).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-pipeline-depth/06-CONTEXT.md
@.planning/phases/06-content-pipeline-depth/06-RESEARCH.md
@.planning/phases/06-content-pipeline-depth/06-01-SUMMARY.md

@src/components/pipeline/ContentReviewModal.tsx
@src/hooks/useContentReviews.ts
@src/hooks/useAiRefinement.ts
@src/hooks/useN8nWebhook.ts
@src/hooks/useContentVersions.ts
@src/hooks/useContentPerformance.ts
@src/types/database.ts

<interfaces>
From src/hooks/useAiRefinement.ts (Plan 01):
```typescript
export function useAiRefinement(): UseMutationResult<AiRefinementResult | { hashtags: string[] }, Error, { caption: string, action: 'refine' | 'hashtags' }>
```

From src/hooks/useN8nWebhook.ts (Plan 01):
```typescript
export function useN8nWebhook(): { fireWebhook: (payload: { action: 'approved' | 'rejected', post_title: string, scheduled_date: string | null, platform: string, rejection_reason?: string }) => Promise<void> }
```

From src/hooks/useContentVersions.ts (Plan 01):
```typescript
export function useContentVersions(contentId: string): UseQueryResult<ContentVersion[]>
export function useCreateVersion(): UseMutationResult
export function useRevertVersion(): UseMutationResult
```

From src/hooks/useContentPerformance.ts (Plan 01):
```typescript
export function useContentPerformance(contentId: string): UseQueryResult<ContentPerformance | null>
export function useUpsertPerformance(): UseMutationResult
```

From src/hooks/useContentReviews.ts (Plan 01):
```typescript
export function useUpdateCaption(): UseMutationResult<void, Error, { id: string, caption: string }>
```

From src/types/database.ts (Plan 01):
```typescript
export interface AiSuggestion {
  type: 'tighten' | 'strengthen' | 'cut' | 'restructure' | 'cta'
  description: string
  original: string
  revised: string
}
export interface ContentVersion { id: string; content_id: string; revision: number; caption: string | null; change_summary: string | null; created_at: string }
export interface ContentPerformance { id: string; content_id: string; impressions: number; likes: number; comments: number; shares: number; click_through_rate: number; engagement_rate: number; recorded_at: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiRefinementPanel, RevisionHistory, and PerformanceCard sub-components</name>
  <read_first>
    - src/components/pipeline/ContentReviewModal.tsx
    - src/hooks/useAiRefinement.ts
    - src/hooks/useContentVersions.ts
    - src/hooks/useContentPerformance.ts
    - src/types/database.ts
  </read_first>
  <files>
    src/components/pipeline/AiRefinementPanel.tsx,
    src/components/pipeline/RevisionHistory.tsx,
    src/components/pipeline/PerformanceCard.tsx
  </files>
  <action>
**1. Create src/components/pipeline/AiRefinementPanel.tsx:**
- Props: `{ caption: string, onApplySuggestion: (original: string, revised: string) => void }`
- Uses `useAiRefinement()` mutation hook
- Two buttons at top: "Refine Caption" and "Suggest Hashtags" (separate per D-12)
- "Refine Caption" button: onClick calls `refinement.mutate({ caption, action: 'refine' })`. While pending, show SkeletonBlock shimmer with "Analyzing..." label. Disable button while pending (per research pitfall 5).
- On success: display 2-3 AiSuggestion cards. Each card shows:
  - Badge with suggestion type (tighten/strengthen/cut/restructure/cta) using appropriate colors
  - Description text (1 sentence)
  - Two-column layout: left shows original text with red-ish background `bg-[hsl(var(--status-error)/0.08)]`, right shows revised text with green-ish background `bg-[hsl(var(--status-success)/0.08)]`
  - "Apply" button (small, outline) calls onApplySuggestion(original, revised) which does string replace in parent
  - "Dismiss" button (small, ghost) removes this suggestion from the list
- If result includes `in_range: false`, show a warning banner: "{range_note}" with amber background
- "Suggest Hashtags" button: onClick calls `refinement.mutate({ caption, action: 'hashtags' })`. Shows result as a row of clickable Badge components. Click copies individual hashtag to clipboard via navigator.clipboard. "Copy All" button copies all hashtags joined with spaces.
- Per D-09: the AI prompt (in the hook) enforces Build What Lasts voice. The panel just displays results.

**2. Create src/components/pipeline/RevisionHistory.tsx:**
- Props: `{ contentId: string, currentCaption: string | null }`
- First: `npm install diff && npm install -D @types/diff` (should be done in Plan 01, but verify)
- Uses `useContentVersions(contentId)` to fetch versions
- Uses `useRevertVersion()` mutation
- Renders a collapsible section with header "Revision History ({count})"
- Each version row: "v{revision}" badge, date formatted with date-fns formatDistanceToNow, change_summary text (or "Initial version" if null)
- "Compare" button on each version: toggles a diff view below the version row
- Diff view: imports `diffWords` from 'diff'. Compares selected version's caption against currentCaption. Renders spans: added text with `bg-[hsl(var(--status-success)/0.15)] text-[hsl(var(--status-success))]`, removed text with `bg-[hsl(var(--status-error)/0.15)] text-[hsl(var(--status-error))] line-through`
- "Revert" button on each version (except latest): calls revertVersion.mutate({ contentId, versionId: version.id, caption: version.caption }). Shows confirmation dialog first.
- Empty state: "No revision history yet"

**3. Create src/components/pipeline/PerformanceCard.tsx:**
- Props: `{ contentId: string }`
- Uses `useContentPerformance(contentId)` and `useUpsertPerformance()`
- Only renders when called (parent controls visibility based on status='posted' per D-32)
- If no performance data exists: show "Add Performance Metrics" button that reveals the form
- Form fields (all numeric inputs, styled with shadcn Input): Impressions, Likes, Comments, Shares, Engagement Rate (%)
- Submit button calls upsertPerformance.mutate with content_id. Click-through rate computed as (clicks / impressions * 100) but since we don't have clicks field, leave click_through_rate at 0 for now.
- If data exists: display as a compact stat grid: 2x3 grid with large numbers and small labels (same pattern as dashboard stat tiles). "Edit" button toggles back to form.
- "Top performer" logic: not in this component. That badge goes in the month view card (handled separately).
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm install diff @types/diff --save 2>&1 | tail -5 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pipeline/AiRefinementPanel.tsx contains `useAiRefinement`
    - src/components/pipeline/AiRefinementPanel.tsx contains `Refine Caption`
    - src/components/pipeline/AiRefinementPanel.tsx contains `Suggest Hashtags`
    - src/components/pipeline/AiRefinementPanel.tsx contains `onApplySuggestion`
    - src/components/pipeline/AiRefinementPanel.tsx contains `in_range`
    - src/components/pipeline/RevisionHistory.tsx contains `diffWords`
    - src/components/pipeline/RevisionHistory.tsx contains `useContentVersions`
    - src/components/pipeline/RevisionHistory.tsx contains `useRevertVersion`
    - src/components/pipeline/RevisionHistory.tsx contains `line-through`
    - src/components/pipeline/PerformanceCard.tsx contains `useContentPerformance`
    - src/components/pipeline/PerformanceCard.tsx contains `useUpsertPerformance`
    - src/components/pipeline/PerformanceCard.tsx contains `impressions`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>3 sub-components created: AI refinement with suggestion cards and hashtag display, revision history with diff viewer and revert, performance metrics with entry form and stat grid. All compile.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ContentReviewModal with edit mode, AI panel, n8n webhooks, revisions, performance, carousel preview</name>
  <read_first>
    - src/components/pipeline/ContentReviewModal.tsx
    - src/components/pipeline/AiRefinementPanel.tsx
    - src/components/pipeline/RevisionHistory.tsx
    - src/components/pipeline/PerformanceCard.tsx
    - src/hooks/useContentReviews.ts
    - src/hooks/useN8nWebhook.ts
    - src/hooks/useContentVersions.ts
  </read_first>
  <files>src/components/pipeline/ContentReviewModal.tsx</files>
  <action>
**Extend ContentReviewModal.tsx with the following features:**

**A. Caption Edit Mode (D-28, D-29, D-30):**
- Add state: `const [isEditing, setIsEditing] = useState(false)` and `const [editCaption, setEditCaption] = useState(item.caption ?? '')`
- Add "Edit" / "Done" toggle button next to the caption section header
- In edit mode: replace the read-only caption text with a `<Textarea>` (shadcn) with value={editCaption}, onChange handler, rows=10, className for full width
- Character count indicator below textarea: use existing CharCountIndicator component. Per D-29: green 1200-1600, amber outside, red >3000.
- Auto-save on blur (per D-30): onBlur handler checks if editCaption !== lastSavedCaption. If changed: call useUpdateCaption().mutate({ id: item.id, caption: editCaption }). Also call useCreateVersion().mutate({ content_id: item.id, revision: item.revision + 1, caption: editCaption, change_summary: 'Manual edit' }). Update lastSavedCaption ref.
- Keep a `lastSavedRef = useRef(item.caption)` to compare against and avoid duplicate saves (per research pitfall 6).

**B. AI Refinement (D-07, D-08, D-09, D-10, D-11, D-12):**
- Below the caption section (whether editing or viewing), render `<AiRefinementPanel caption={editCaption || item.caption || ''} onApplySuggestion={handleApplySuggestion} />`
- handleApplySuggestion: does `setEditCaption(prev => prev.replace(original, revised))`. Enters edit mode if not already. Shows toast "Suggestion applied".

**C. Multi-platform tabs (CSUG-08):**
- If item.platforms has multiple entries, show tabs above the caption area (one per platform). Currently, just show the platform name and share the same caption. This scaffolds the per-platform view structure but uses shared caption for now. Each tab: Badge with platform name. Active tab highlighted.

**D. Carousel slide preview (CSUG-03):**
- If item.content_type === 'carousel' and item.slide_count > 0: render a horizontal scroll row of placeholder boxes below the caption area
- Each box: 120px height, 160px width, rounded-[var(--radius-md)], bg-[hsl(var(--bg-elevated))], border dashed, numbered "Slide 1", "Slide 2", etc. up to slide_count
- If item.slides array exists and has entries: show slide title and description inside each box instead of just the number
- "Export from Excalidraw" button (disabled, ghost) below the row as a placeholder for future pipeline

**E. n8n Webhook on Approve/Reject (D-14, D-15, D-16):**
- Import useN8nWebhook
- In the existing approve handler (after calling useUpdateContentStatus with status='approved'): call `fireWebhook({ action: 'approved', post_title: item.post_title, scheduled_date: item.scheduled_date, platform: item.platforms[0] ?? 'linkedin' })`
- In the existing reject handler (after calling useUpdateContentStatus with status='rejected'): call `fireWebhook({ action: 'rejected', post_title: item.post_title, scheduled_date: item.scheduled_date, platform: item.platforms[0] ?? 'linkedin', rejection_reason: feedback })`
- Per D-13: one-way only. No Slack response handling.

**F. Revision History (D-34, D-35, D-36):**
- Below the AI refinement panel, render `<RevisionHistory contentId={item.id} currentCaption={editCaption || item.caption} />`

**G. Performance Metrics (D-31, D-32, D-33):**
- Only when item.status === 'posted': render `<PerformanceCard contentId={item.id} />`
- Place it in a distinct section with "Performance" header

**H. Publishing time scaffold (CSUG-10):**
- Below the scheduled date display, add a time input `<input type="time" />` styled with shadcn Input. Default value from item metadata or empty. On change, store locally (not persisted yet -- scaffold only). Label: "Publish time (coming soon)" with muted text.

**Layout order in modal (top to bottom):**
1. Header (title, status badge, platform tags, dates) -- existing
2. Multi-platform tabs (if multiple platforms)
3. Caption section with edit toggle + character count
4. AI Refinement Panel
5. Carousel slide preview (if carousel)
6. Publishing time scaffold
7. Revision History (collapsible)
8. Performance Metrics (if posted)
9. Approve/Reject action buttons -- existing
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pipeline/ContentReviewModal.tsx contains `isEditing`
    - src/components/pipeline/ContentReviewModal.tsx contains `useUpdateCaption`
    - src/components/pipeline/ContentReviewModal.tsx contains `AiRefinementPanel`
    - src/components/pipeline/ContentReviewModal.tsx contains `RevisionHistory`
    - src/components/pipeline/ContentReviewModal.tsx contains `PerformanceCard`
    - src/components/pipeline/ContentReviewModal.tsx contains `useN8nWebhook`
    - src/components/pipeline/ContentReviewModal.tsx contains `fireWebhook`
    - src/components/pipeline/ContentReviewModal.tsx contains `onBlur`
    - src/components/pipeline/ContentReviewModal.tsx contains `lastSaved`
    - src/components/pipeline/ContentReviewModal.tsx contains `Slide`
    - src/components/pipeline/ContentReviewModal.tsx contains `type="time"`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>ContentReviewModal extended with: editable caption + auto-save, AI refinement panel, hashtag suggestions, n8n webhook on approve/reject, revision history with diff, performance metrics for posted items, carousel slide placeholders, publishing time scaffold. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Modal has edit/view toggle for caption
- AI refinement button visible (requires VITE_ANTHROPIC_API_KEY to function)
- n8n webhook fires on approve/reject (requires VITE_N8N_WEBHOOK_URL to function)
- Revision history renders for items with versions
- Performance card only appears for posted items
</verification>

<success_criteria>
- Caption editable with character count and auto-save on blur
- AI refinement returns 2-3 suggestions that can be individually applied
- Hashtag suggestions are separate from refinement (per D-12)
- n8n webhook fires on approve/reject (graceful skip if URL not configured)
- Revision history shows versions with diff comparison
- Performance metrics enterable for posted items
- Carousel slide placeholders render for carousel content
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline-depth/06-03-SUMMARY.md`
</output>
