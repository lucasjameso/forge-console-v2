---
phase: 06-content-pipeline-depth
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - src/pages/ContentPipeline.tsx
  - src/components/pipeline/ContentCard.tsx
autonomous: false
requirements:
  - CONT-02
  - CSUG-01
  - CSUG-10

must_haves:
  truths:
    - "Calendar navigation with slide animations works across month and week views per CSUG-01"
    - "Top performer badge appears on highest-engagement posted item in month view per D-33"
    - "All 5 views render without errors after full integration"
    - "ContentCard shows performance badge for posted items with metrics"
    - "Build passes with all features wired together"
  artifacts:
    - path: "src/pages/ContentPipeline.tsx"
      provides: "Fully integrated Content Pipeline page with all Phase 6 features"
      contains: "DndContext"
    - path: "src/components/pipeline/ContentCard.tsx"
      provides: "Extended card with performance badge"
      contains: "Top performer"
  key_links:
    - from: "src/pages/ContentPipeline.tsx"
      to: "src/components/pipeline/TemplatesTab.tsx"
      via: "templates view mode"
      pattern: "TemplatesTab"
    - from: "src/pages/ContentPipeline.tsx"
      to: "src/components/pipeline/AnalyticsStrip.tsx"
      via: "analytics strip above views"
      pattern: "AnalyticsStrip"
    - from: "src/pages/ContentPipeline.tsx"
      to: "@dnd-kit/core"
      via: "DndContext for Kanban and Month views"
      pattern: "DndContext"
---

<objective>
Final integration pass: polish calendar navigation animations, add "Top performer" badge to month view cards, verify all 5 views work together, fix any integration issues from parallel plans 02/03/04, and do visual QA checkpoint.

Purpose: Ensure all Phase 6 features work together as a cohesive content management experience.
Output: Polished ContentPipeline page, extended ContentCard, visual QA verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-pipeline-depth/06-CONTEXT.md
@.planning/phases/06-content-pipeline-depth/06-01-SUMMARY.md
@.planning/phases/06-content-pipeline-depth/06-02-SUMMARY.md
@.planning/phases/06-content-pipeline-depth/06-03-SUMMARY.md
@.planning/phases/06-content-pipeline-depth/06-04-SUMMARY.md

@src/pages/ContentPipeline.tsx
@src/components/pipeline/ContentCard.tsx
@src/components/pipeline/ContentReviewModal.tsx
@src/hooks/useContentReviews.ts
@src/hooks/useContentPerformance.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish calendar navigation animations, add Top Performer badge, fix integration issues</name>
  <read_first>
    - src/pages/ContentPipeline.tsx
    - src/components/pipeline/ContentCard.tsx
    - src/hooks/useContentPerformance.ts
    - src/types/database.ts
  </read_first>
  <files>
    src/pages/ContentPipeline.tsx,
    src/components/pipeline/ContentCard.tsx
  </files>
  <action>
**1. Calendar navigation slide animations (CSUG-01):**
- The month/week navigation arrows already exist from Phase 4. Enhance with Framer Motion slide transitions:
- Wrap month view grid in `<AnimatePresence mode="wait">` with key={currentMonth.toISOString()}
- On month change: content slides left (going forward) or right (going back) using motion.div initial={{ x: direction * 100, opacity: 0 }} animate={{ x: 0, opacity: 1 }} exit={{ x: direction * -100, opacity: 0 }} transition={{ type: 'spring', stiffness: 300, damping: 30, duration: 0.2 }}
- Track direction state: `const [slideDirection, setSlideDirection] = useState(1)`. Set to 1 on forward, -1 on back.
- Apply same pattern to week view content area.
- "Today" button resets to current month/week with direction based on whether target is before or after current.

**2. Top Performer badge (D-33):**
- In the month view rendering, determine which posted item in the current month has the highest engagement_rate.
- This requires performance data. Add a useQuery for all content_performance in the month (or fetch all and filter client-side).
- Simpler approach: add a prop to ContentCard/DraggableContentCard: `isTopPerformer: boolean`
- In ContentPipeline.tsx month view section: compute topPerformerId by looking at items with status='posted' that have associated performance data. For now, since performance data may not exist yet, make this a placeholder that shows the badge if a `topPerformerId` is passed.
- Extend src/components/pipeline/ContentCard.tsx: if `isTopPerformer` prop is true, render a small gold star badge or "Top" Badge with variant matching success color. Position at top-right of the card.

**3. Integration fixes:**
- Read through ContentPipeline.tsx after plans 02, 03, 04 have executed. Fix any:
  - Import conflicts (duplicate imports from parallel plans)
  - State management conflicts (multiple plans may have added state to ContentPipeline.tsx)
  - Layout issues (analytics strip + view content + bulk action bar layering)
- Ensure AnalyticsStrip renders between header controls and view content
- Ensure BulkActionBar renders in a fixed position that doesn't overlap with content
- Ensure DndContext only wraps Kanban and Month views, NOT List, Week, or Templates views
- Ensure the Templates view mode works alongside the drag-drop views without interference

**4. ContentPipeline.tsx cleanup if needed:**
- If the file exceeds ~1000 lines, extract the largest view sections into separate files:
  - KanbanView as a separate component if needed
  - MonthView as a separate component if needed
- Keep the main page file as the orchestrator that imports view components
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ContentPipeline.tsx contains `AnimatePresence`
    - src/pages/ContentPipeline.tsx contains `slideDirection` or `direction`
    - src/components/pipeline/ContentCard.tsx contains `isTopPerformer` or `topPerformer`
    - src/components/pipeline/ContentCard.tsx contains `Top` (for badge label)
    - src/pages/ContentPipeline.tsx contains `TemplatesTab`
    - src/pages/ContentPipeline.tsx contains `AnalyticsStrip`
    - src/pages/ContentPipeline.tsx contains `BulkActionBar`
    - src/pages/ContentPipeline.tsx contains `DndContext`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Calendar navigation has slide animations. Top Performer badge renders on highest-engagement posted item. All Phase 6 features integrated without conflicts. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA of all Content Pipeline features</name>
  <what-built>Complete Phase 6 Content Pipeline transformation: drag-drop in Kanban/Month, caption editing with AI refinement, templates tab with week generation, analytics strip, bulk selection, revision history, performance tracking, carousel preview placeholders.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/content in browser
    2. **View modes**: Verify all 5 tabs work (List, Week, Month, Kanban, Templates)
    3. **Kanban drag**: Drag a card between status columns. Verify toast appears and status updates.
    4. **Month drag**: Drag a card between date cells. Verify toast and date updates.
    5. **Click-to-move**: Verify dropdown menu still works in Kanban alongside drag.
    6. **Bulk select**: Check boxes on 2-3 items. Verify floating action bar appears at bottom.
    7. **Analytics strip**: Verify counts and cadence indicator display above views.
    8. **Detail modal**: Click a card to open modal. Verify:
       - Edit button toggles caption to editable textarea
       - Character count shows with color coding
       - AI Refinement panel visible (may need API key to test)
       - Revision History section visible
       - For posted items: Performance metrics section visible
       - For carousel items: Slide preview placeholders visible
    9. **Templates tab**: Verify 6 templates listed (Mon-Sat). Verify Generate Week button works.
    10. **Calendar animations**: Navigate months forward/back. Verify slide animation.
    11. Report any issues or type "approved"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- All 5 view modes render correctly
- Drag-and-drop works in Kanban and Month (not in Week/List/Templates)
- Modal features all accessible
- Analytics strip visible and accurate
- Templates tab functional
</verification>

<success_criteria>
- All Phase 6 roadmap success criteria met:
  1. Calendar navigation allows browsing months forward/back with slide animations and "Today" button
  2. Content items can be dragged between days (month) and columns (kanban) with Supabase update and toast
  3. Caption editing in detail modal with character count and "Refine with AI" integration
  4. Bulk actions work via floating action bar when items selected
  5. Analytics strip shows monthly summary, cadence indicator, and gap alerts
- User visually confirms the feature set works as expected
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pipeline-depth/06-05-SUMMARY.md`
</output>
