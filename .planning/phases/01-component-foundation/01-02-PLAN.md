---
phase: 01-component-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/table.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/SkeletonBlock.tsx
  - src/styles/globals.css
autonomous: true
requirements: [FOUN-02]

must_haves:
  truths:
    - "All shadcn component files exist in src/components/ui/ and export their components"
    - "Existing Badge component replaced with shadcn Badge extended with Forge Console variants (success, warning, error, info, navy, coral, neutral)"
    - "Existing SkeletonBlock replaced with shadcn Skeleton, with a SkeletonBlock compatibility wrapper"
    - "Custom CSS component classes for buttons, cards, badges, inputs, and skeletons removed from globals.css"
    - "Build passes with no TypeScript errors"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card component"
      exports: ["Card", "CardContent", "CardHeader", "CardTitle", "CardDescription", "CardFooter"]
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge with Forge Console custom variants"
      exports: ["Badge", "badgeVariants"]
    - path: "src/components/ui/skeleton.tsx"
      provides: "shadcn Skeleton component"
      exports: ["Skeleton"]
    - path: "src/components/ui/sonner.tsx"
      provides: "shadcn Sonner toast wrapper"
      exports: ["Toaster"]
  key_links:
    - from: "src/components/ui/badge.tsx"
      to: "src/styles/globals.css"
      via: "CSS variables for status colors"
      pattern: "hsl\\(var\\(--status-"
    - from: "src/components/ui/skeleton.tsx"
      to: "src/styles/globals.css"
      via: "Animation styles from tailwindcss-animate"
      pattern: "animate-pulse"
---

<objective>
Install all shadcn/ui components, replace existing Badge and SkeletonBlock with shadcn equivalents, and remove orphaned custom CSS component classes from globals.css.

Purpose: This makes shadcn primitives available for use across the entire app. The Badge and Skeleton replacements are done now (per D-06, D-07) so that no code depends on the old custom implementations. CSS cleanup (per D-09) removes dead classes immediately.

Output: 20+ shadcn component files in src/components/ui/, updated Badge with custom variants, Skeleton replacement, cleaned globals.css with component classes removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-component-foundation/01-CONTEXT.md
@.planning/phases/01-component-foundation/01-RESEARCH.md
@.planning/phases/01-component-foundation/01-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, globals.css has HSL tokens and shadcn bridge variables -->
<!-- components.json exists with correct aliases -->
<!-- tailwindcss-animate plugin is active -->

From src/components/ui/Badge.tsx (WILL BE REPLACED):
```typescript
type BadgeVariant = 'success' | 'warning' | 'error' | 'info' | 'navy' | 'coral' | 'neutral'
export function Badge({ children, variant = 'neutral', className }: BadgeProps)
```

From src/components/ui/SkeletonBlock.tsx (WILL BE REPLACED):
```typescript
export function SkeletonBlock({ width, height = 16, className, style }: SkeletonBlockProps)
export function SkeletonCard({ className }: { className?: string })
```

Files that import Badge: grep shows ~9 files across pages and components
Files that import SkeletonBlock/SkeletonCard: ~11 files across pages and components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install all shadcn components via CLI and extend Badge with custom variants</name>
  <files>
    src/components/ui/button.tsx,
    src/components/ui/card.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/input.tsx,
    src/components/ui/label.tsx,
    src/components/ui/select.tsx,
    src/components/ui/separator.tsx,
    src/components/ui/tabs.tsx,
    src/components/ui/dialog.tsx,
    src/components/ui/tooltip.tsx,
    src/components/ui/switch.tsx,
    src/components/ui/checkbox.tsx,
    src/components/ui/progress.tsx,
    src/components/ui/table.tsx,
    src/components/ui/dropdown-menu.tsx,
    src/components/ui/sheet.tsx,
    src/components/ui/sonner.tsx,
    src/components/ui/alert.tsx,
    src/components/ui/scroll-area.tsx,
    src/components/ui/skeleton.tsx,
    src/components/ui/textarea.tsx
  </files>
  <read_first>
    src/components/ui/Badge.tsx,
    src/components/ui/SkeletonBlock.tsx,
    components.json,
    src/styles/globals.css
  </read_first>
  <action>
1. Delete the old Badge.tsx and SkeletonBlock.tsx before running shadcn CLI (the CLI will generate lowercase replacements):
   ```bash
   rm src/components/ui/Badge.tsx src/components/ui/SkeletonBlock.tsx
   ```

2. Run the shadcn CLI to install ALL component groups (per D-05). Run as a single command:
   ```bash
   npx shadcn@latest add button card badge input label select separator tabs dialog tooltip switch checkbox progress table dropdown-menu sheet sonner alert scroll-area skeleton textarea --yes --overwrite
   ```
   The `--yes` flag skips prompts. The `--overwrite` flag handles any conflicts with existing files.

   IMPORTANT: After the CLI runs, verify that `tailwindcss` in package.json is still `^3.4.19` or `3.4.19`. If it changed to v4, revert with `npm install tailwindcss@3.4.19`.

3. After shadcn generates `src/components/ui/badge.tsx`, extend it with Forge Console custom variants. The generated badge.tsx will have a `badgeVariants` CVA definition with `default`, `secondary`, `destructive`, and `outline` variants. Add these additional variants inside the `variants.variant` object:

   ```typescript
   // Add after the "outline" variant in badgeVariants:
   success: "border-transparent bg-[hsl(var(--status-success-bg))] text-[hsl(var(--status-success))]",
   warning: "border-transparent bg-[hsl(var(--status-warning-bg))] text-[hsl(var(--status-warning))]",
   error: "border-transparent bg-[hsl(var(--status-error-bg))] text-[hsl(var(--status-error))]",
   info: "border-transparent bg-[hsl(var(--status-info-bg))] text-[hsl(var(--status-info))]",
   navy: "border-transparent bg-[hsl(var(--accent-navy)/0.1)] text-[hsl(var(--accent-navy))]",
   coral: "border-transparent bg-[hsl(var(--accent-coral)/0.1)] text-[hsl(var(--accent-coral))]",
   neutral: "border-transparent bg-secondary text-muted-foreground",
   ```

   Also update the TypeScript type to include these variants. The generated Badge component likely uses `VariantProps<typeof badgeVariants>` which will automatically include the new variants, but verify the exported type includes all 11 variants (default, secondary, destructive, outline, success, warning, error, info, navy, coral, neutral).

4. Update all files that import from `@/components/ui/Badge` (capital B) to import from `@/components/ui/badge` (lowercase b). Search for all imports:
   ```bash
   grep -rl "from '@/components/ui/Badge'" src/
   ```
   In each file, change the import path from `'@/components/ui/Badge'` to `'@/components/ui/badge'`.

   The old Badge API was `<Badge variant="success">`. The new shadcn Badge API is the same: `<Badge variant="success">`. No prop changes needed since we preserved the variant names.

5. Create a compatibility wrapper for SkeletonBlock so existing consumers do not break. Create `src/components/ui/SkeletonBlock.tsx`:

   ```typescript
   import { Skeleton } from '@/components/ui/skeleton'
   import { cn } from '@/lib/utils'

   interface SkeletonBlockProps {
     width?: string | number
     height?: string | number
     className?: string
     style?: React.CSSProperties
   }

   export function SkeletonBlock({ width, height = 16, className, style }: SkeletonBlockProps) {
     const w = typeof width === 'number' ? `${width}px` : width
     const h = typeof height === 'number' ? `${height}px` : height

     return (
       <Skeleton
         className={cn(className)}
         style={{
           width: w,
           height: h,
           ...style,
         }}
       />
     )
   }

   export function SkeletonCard({ className }: { className?: string }) {
     return (
       <div className={cn('rounded-lg border bg-card p-6 shadow-card', className)} style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
         <Skeleton className="h-[18px] w-[60%]" />
         <Skeleton className="h-[12px] w-full" />
         <Skeleton className="h-[12px] w-[80%]" />
         <Skeleton className="h-[12px] w-[40%]" />
       </div>
     )
   }
   ```

   Note: SkeletonCard no longer uses the `.card` CSS class -- it uses Tailwind utilities that match the shadcn Card styling. This allows us to remove the `.card` CSS class in Task 2.

6. Verify `npm run build` passes.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/button.tsx exists and exports `Button`
    - src/components/ui/card.tsx exists and exports `Card`
    - src/components/ui/badge.tsx exists and contains `success:` variant
    - src/components/ui/badge.tsx contains `navy:` variant
    - src/components/ui/badge.tsx contains `coral:` variant
    - src/components/ui/badge.tsx contains `neutral:` variant
    - src/components/ui/skeleton.tsx exists and exports `Skeleton`
    - src/components/ui/sonner.tsx exists and exports `Toaster`
    - src/components/ui/SkeletonBlock.tsx exists and imports from `@/components/ui/skeleton`
    - No file in src/ imports from `@/components/ui/Badge` (capital B) -- all use lowercase `badge`
    - tailwindcss version in package.json is 3.x (not 4.x)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    All shadcn components installed (per D-05). Badge extended with 7 Forge Console variants (per D-07). SkeletonBlock wraps shadcn Skeleton (per D-06). Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned custom CSS component classes from globals.css</name>
  <files>
    src/styles/globals.css
  </files>
  <read_first>
    src/styles/globals.css,
    src/components/ui/badge.tsx,
    src/components/ui/skeleton.tsx,
    src/components/ui/SkeletonBlock.tsx
  </read_first>
  <action>
Per D-09: Remove custom CSS component classes that are now replaced by shadcn primitives. Edit `src/styles/globals.css` and remove these class definitions from the `@layer components` block:

**REMOVE all of these:**
- `.card` and `.card:hover` (lines ~124-137) -- replaced by shadcn Card
- `.btn-primary`, `.btn-primary:hover`, `.btn-primary:disabled`, `.btn-primary:disabled:hover` (lines ~139-309) -- replaced by shadcn Button
- `.btn-ghost`, `.btn-ghost:hover` (lines ~159-177) -- replaced by shadcn Button variant="ghost"
- `.badge` base class (lines ~179-189) -- replaced by shadcn Badge
- `.badge-success`, `.badge-warning`, `.badge-error`, `.badge-info`, `.badge-navy`, `.badge-coral`, `.badge-neutral` (lines ~192-225) -- replaced by shadcn Badge variants
- `.input`, `.input:focus`, `.input::placeholder` (lines ~227-248) -- replaced by shadcn Input
- `.skeleton` and `@keyframes skeleton-shimmer` (lines ~250-266) -- replaced by shadcn Skeleton
- `.btn-primary:focus-visible`, `.btn-ghost:focus-visible`, `.input:focus-visible` (lines ~311-317) -- shadcn components handle their own focus states

**KEEP all of these (not replaced by shadcn):**
- `.text-page-title`, `.text-section-header`, `.text-card-title`, `.text-body`, `.text-caption`, `.text-stat` -- typography hierarchy classes (no shadcn equivalent)
- `.status-dot`, `.status-dot-success`, `.status-dot-warning`, `.status-dot-error`, `.status-dot-info` -- custom component per D-08
- `::-webkit-scrollbar` styles -- custom scrollbar styling
- `::selection` -- selection color styling

After removal, the `@layer components` block should contain ONLY:
1. Typography classes (.text-page-title through .text-stat)
2. Status dot classes (.status-dot and variants)
3. Scrollbar styles
4. Selection color

Also verify that the `@layer base` block is intact (*, html, body, heading styles).

Run `npm run build` to confirm nothing breaks. If build fails because some file still uses a removed CSS class directly (like `className="card"`), that file needs to be updated to use Tailwind utilities or shadcn components. Check with:
```bash
grep -rn 'className="card"' src/ --include='*.tsx'
grep -rn "className='card'" src/ --include='*.tsx'
```
If matches found outside of SkeletonBlock.tsx, replace `className="card"` with `className="rounded-lg border bg-card p-6 shadow-card"` or wrap in a shadcn `<Card>` component.
  </action>
  <verify>
    <automated>cd /Users/lucas/Forge/Projects/forge-console-v2 && npm run build && grep -c "\.btn-primary" src/styles/globals.css && grep -c "\.badge-success" src/styles/globals.css && grep -c "\.skeleton" src/styles/globals.css; echo "done"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css does NOT contain `.btn-primary`
    - globals.css does NOT contain `.btn-ghost`
    - globals.css does NOT contain `.badge {` (base badge class)
    - globals.css does NOT contain `.badge-success`
    - globals.css does NOT contain `.input {` (bare input class, not `.input:`)
    - globals.css does NOT contain `.skeleton {`
    - globals.css does NOT contain `@keyframes skeleton-shimmer`
    - globals.css does NOT contain `.card {` (bare card class)
    - globals.css DOES contain `.text-page-title`
    - globals.css DOES contain `.text-section-header`
    - globals.css DOES contain `.status-dot`
    - globals.css DOES contain `::selection`
    - globals.css DOES contain `::-webkit-scrollbar`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    All custom CSS component classes replaced by shadcn primitives have been removed from globals.css (per D-09). Only typography, status-dot, scrollbar, and selection styles remain. No orphaned CSS. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `ls src/components/ui/*.tsx | wc -l` shows 20+ component files
- `grep 'success:' src/components/ui/badge.tsx` matches (custom variant preserved)
- `grep '\.btn-primary' src/styles/globals.css` returns no matches
- `grep '\.badge-success' src/styles/globals.css` returns no matches
- `grep '\.card {' src/styles/globals.css` returns no matches (card CSS class gone, but `.card` references in Tailwind are fine)
- `grep 'text-page-title' src/styles/globals.css` matches (typography classes preserved)
</verification>

<success_criteria>
- All shadcn component groups installed per D-05 (button, card, badge, input, label, select, separator, tabs, dialog, tooltip, switch, checkbox, progress, table, dropdown-menu, sheet, sonner, alert, scroll-area, skeleton, textarea)
- Badge preserves all 7 Forge Console variants (per D-07)
- SkeletonBlock wraps shadcn Skeleton (per D-06)
- All replaced CSS classes removed from globals.css (per D-09)
- Typography and status-dot classes preserved (per D-08, research notes)
- Tailwind stays on v3.4.19
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-component-foundation/01-02-SUMMARY.md`
</output>
